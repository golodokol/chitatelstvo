#allrecords .t-rec[data-record-type="762"],
#allrecords .t-rec[data-record-type="205"],
#rec2380183631,
#rec2380172391,
#rec2380172421 {
  display: none !important;
  height: 0 !important;
  min-height: 0 !important;
  max-height: 0 !important;
  padding: 0 !important;
  margin: 0 !important;
  overflow: hidden !important;
  visibility: hidden !important;
  border: 0 !important;
}

/* Tilda Zero: не обрезать длинную страницу и не держать контент invisible */
    .tn-atom__html,
    .tn-atom__html #chit-main {
      height: auto !important;
      min-height: 0 !important;
      overflow: visible !important;
    }
    .t396__artboard,
    .t396__carrier,
    .t396__filter,
    .tn-elem[data-elem-type="html"] {
      overflow: visible !important;
    }
    .t396__artboard,
    .t396__carrier,
    .t396__filter {
      height: auto !important;
      min-height: 0 !important;
      background-color: #F6F4F9 !important;
    }
    @media screen and (max-width: 1199px),
    screen and (max-width: 959px),
    screen and (max-width: 639px),
    screen and (max-width: 479px) {
      .t396__artboard,
      .t396__carrier,
      .t396__filter,
      .tn-elem[data-elem-type="html"] {
        height: auto !important;
        min-height: 0 !important;
        overflow: visible !important;
      }
    }
    .t396__artboard.rendering .tn-elem {
      visibility: visible !important;
    }

#chit-main {
  --blue: #5B7FA6;
  --blue-light: #88A9D1;
  --blue-pale: #E8F1F8;
  --blue-deep: #4A6D94;
  --accent: #8F7DA3;
  --accent-pale: #EBE6F3;
  --accent-soft: #A896B8;
  --cream: #F6F4F9;
  --cream-warm: #EFEBF5;
  --text: #3D5266;
  --text-warm: #3A4553;
  --muted: #6B8499;
  --border: #D4E2EF;
  --white: #FFFFFF;
  --shadow: 0 4px 24px rgba(61, 82, 102, 0.08);
  --shadow-lg: 0 12px 40px rgba(61, 82, 102, 0.12);
  --radius: 16px;
  --max: 1120px;
  --serif: 'Literata', Georgia, serif;
  --hand: 'Caveat', cursive;
  width: 100%;
  max-width: none;
  margin: 0;
  overflow-x: hidden;
  scroll-behavior: smooth;
  font-family: 'Nunito', system-ui, sans-serif;
  color: var(--text);
  background: var(--cream);
  line-height: 1.6;
  font-size: 17px;
}
    *, *::before, *::after { box-sizing: border-box; }
    .paper-bg {
      background-color: var(--cream);
      background-image:
        radial-gradient(ellipse at 20% 50%, rgba(143,125,163,0.06) 0%, transparent 50%),
        radial-gradient(ellipse at 80% 20%, rgba(91,127,166,0.04) 0%, transparent 40%);
    }

    /* Header */
    .site-header {
      position: sticky; top: 0; z-index: 100;
      background: rgba(255,255,255,0.95);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid var(--border);
    }
    .site-header__inner {
      max-width: var(--max); margin: 0 auto;
      padding: 14px 24px;
      display: flex; align-items: center; gap: 20px;
    }
    .logo {
      flex-shrink: 0;
      display: inline-flex;
      align-items: center;
      text-decoration: none;
      line-height: 0;
    }
    .logo__img {
      height: 72px;
      width: auto;
      display: block;
    }
    .logo.is-sparkle .logo__img {
      animation: logo-sparkle .45s ease;
    }
    @keyframes logo-sparkle {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.06); }
    }
    .header-cta {
      background: var(--blue); color: #fff;
      padding: 10px 22px; border-radius: 999px;
      font-weight: 700; font-size: 15px; text-decoration: none;
      transition: background .2s;
    }
    .header-cta:hover { background: #4A6D94; }
    .site-nav {
      flex: 1;
      display: flex; align-items: center; justify-content: center;
      gap: 28px; flex-wrap: wrap;
    }
    .site-nav a {
      font-size: 14px; font-weight: 700; color: var(--muted);
      text-decoration: none; white-space: nowrap;
      transition: color .15s;
    }
    .site-nav a:hover { color: var(--blue); }
    .site-header__right { flex-shrink: 0; }
    @media (max-width: 820px) {
      .site-nav { display: none; }
      .site-header__inner { justify-content: space-between; }
    }

    .preview-strip {
      background: var(--accent-pale); color: var(--blue-deep);
      text-align: center; padding: 8px 16px;
      font-size: 13px; font-weight: 700;
      border-bottom: 1px solid rgba(143,125,163,0.15);
    }

    /* Buttons */
    .btn {
      display: inline-flex; align-items: center; justify-content: center;
      background: var(--blue); color: #fff;
      padding: 16px 32px; border-radius: 999px;
      font-weight: 700; font-size: 17px; text-decoration: none;
      border: none; cursor: pointer; font-family: inherit;
      transition: background .2s, transform .15s;
    }
    .btn:hover { background: #4A6D94; transform: translateY(-1px); }
    .btn--block { width: 100%; }
    .btn--outline {
      background: transparent; color: var(--blue);
      border: 2px solid var(--blue-light);
      -webkit-text-fill-color: var(--blue);
    }
    .btn--outline:hover { background: var(--blue-pale); }
    .btn--pay {
      font-size: 18px; padding: 18px 32px;
    }
    .btn--hero {
      font-size: 17px; padding: 16px 28px;
      box-shadow: 0 8px 24px rgba(91,127,166,0.28);
    }
    .btn--hero:hover { box-shadow: 0 12px 32px rgba(91,127,166,0.35); }
    .hero--premium .btn--hero { width: auto; }
    .btn:disabled {
      background: #B8C9DB; cursor: not-allowed; transform: none;
    }
    .pay-note {
      text-align: center; margin-top: 12px;
      font-size: 13px; color: var(--muted);
    }

    /* Sections */
    .section { padding: 72px 24px; }
    .section--cream { background: var(--cream); }
    .section--cream.paper-bg,
    .hero.paper-bg { background-color: var(--cream); background-image: radial-gradient(ellipse at 30% 40%, rgba(143,125,163,0.05) 0%, transparent 55%), radial-gradient(ellipse at 70% 60%, rgba(91,127,166,0.05) 0%, transparent 45%); }
    .section--pale { background: #FAFBFC; }
    .section__inner { max-width: var(--max); margin: 0 auto; padding-left: 24px; padding-right: 24px; }

    /* Секции на всю ширину родителя (без 100vw — он ломает мобилу) */
    .preview-strip,
    .hero,
    .section,
    .footer,
    .pattern-strip,
    .final-cta-scroller,
    .benefits,
    .quote-marquee {
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
    }

    #chit-main img,
    #chit-main video,
    #chit-main svg {
      max-width: 100%;
      height: auto;
    }
    #chit-main .founder__photo {
      height: 100% !important;
      min-height: 100%;
      max-height: none;
    }
    .section__chapter {
      display: block; text-align: center;
      font-size: 14px; font-weight: 600;
      letter-spacing: 0.04em;
      color: var(--muted); margin-bottom: 12px;
    }
    .section__chapter em {
      font-style: normal; color: var(--accent); font-weight: 800;
    }
    .section__label {
      display: none;
    }
    .section__title {
      text-align: center; font-size: 36px; font-weight: 800;
      color: var(--blue); margin: 0 0 16px; line-height: 1.2;
      letter-spacing: -0.02em;
    }
    .section__lead {
      text-align: center; max-width: 680px; margin: 0 auto 48px;
      color: var(--muted); font-size: 18px; line-height: 1.65;
    }

    /* Hero */
    .hero {
      padding: 40px 24px 48px;
      background: linear-gradient(180deg, var(--cream) 0%, var(--white) 100%);
      position: relative;
    }
    .hero__inner {
      max-width: var(--max); margin: 0 auto;
      display: flex; flex-direction: column; gap: 32px;
      align-items: center;
    }
    .hero__copy { max-width: 640px; text-align: center; width: 100%; }
    .hero__badge {
      display: inline-block;
      background: var(--white); border: 1px solid var(--border);
      padding: 8px 16px; border-radius: 999px;
      font-size: 13px; font-weight: 600; color: var(--muted);
      margin-bottom: 20px;
      max-width: 560px; line-height: 1.45;
    }
    .hero__badge em {
      font-style: normal; color: var(--accent); font-weight: 800;
    }
    .hero h1 {
      font-size: 48px; font-weight: 800; line-height: 1.1;
      color: var(--blue); margin: 0 0 16px;
      letter-spacing: -0.03em;
    }
    @media (max-width: 600px) { .hero h1 { font-size: 34px; } }
    @media (max-width: 520px) {
      .book-stack { gap: 18px; min-height: 190px; }
      .book { width: 92px; height: 132px; }
      .book:nth-child(2) { width: 106px; height: 154px; }
      .hero__showcase { padding: 36px 20px 32px; }
    }
    .hero__subtitle {
      font-size: 20px; color: var(--muted);
      margin: 0 0 28px; line-height: 1.55; max-width: 520px;
    }
    .hero__price {
      font-size: 15px; color: var(--muted); margin-bottom: 24px;
    }
    .hero__price strong {
      font-size: 28px; color: var(--text); font-weight: 800;
    }
    .hero__actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 4px; justify-content: center; }

    /* Интерактивная полка — главный акцент hero */
    .hero__showcase {
      background: var(--white);
      border-radius: 28px;
      box-shadow: var(--shadow-lg);
      padding: 52px 40px 44px;
      border: 2px solid var(--border);
      text-align: center;
      position: relative;
      max-width: 920px;
      width: 100%;
      margin: 8px auto 0;
      transition: border-color .3s, box-shadow .3s;
    }
    .hero__showcase.is-active {
      border-color: var(--accent-soft);
      box-shadow: 0 16px 48px rgba(143,125,163,0.2);
    }
    .hero__showcase-label {
      font-size: 12px; font-weight: 800; letter-spacing: 0.06em;
      text-transform: uppercase; color: var(--accent);
      margin: 0 0 24px;
    }
    .hero__showcase::after {
      content: '';
      position: absolute; bottom: -10px; left: 24px; right: 24px;
      height: 10px;
      background: linear-gradient(180deg, #B8C8D8 0%, #9AADBE 100%);
      border-radius: 0 0 6px 6px;
      z-index: -1;
    }
    .book-stack {
      display: flex; justify-content: center; align-items: flex-end;
      gap: 28px; margin-bottom: 32px; perspective: 700px;
      min-height: 220px;
    }
    .book {
      width: 108px; height: 156px; border-radius: 4px 10px 10px 4px;
      box-shadow: 0 6px 20px rgba(61,82,102,0.18);
      transform-origin: bottom center;
      transition: transform .35s ease, box-shadow .35s ease;
      cursor: pointer;
      overflow: hidden;
      position: relative;
      background: var(--blue-pale);
      border: none;
      padding: 0;
      font: inherit;
    }
    .book__cover {
      width: 100%; height: 100%; object-fit: cover;
      display: block; pointer-events: none;
    }
    .book:nth-child(1) { background: var(--blue); transform: rotate(-10deg); }
    .book:nth-child(2) { background: var(--accent-soft); transform: rotate(0deg) translateY(-14px); height: 180px; width: 124px; }
    .book:nth-child(3) { background: var(--blue-light); transform: rotate(10deg); }
    .book:hover { box-shadow: 0 10px 28px rgba(61,82,102,0.22); }
    .book.is-active {
      transform: rotate(0deg) translateY(-16px) scale(1.06) !important;
      box-shadow: 0 14px 32px rgba(143,125,163,0.28);
      z-index: 2;
    }
    .book:nth-child(1).is-active { transform: rotate(-4deg) translateY(-16px) scale(1.06) !important; }
    .book:nth-child(3).is-active { transform: rotate(4deg) translateY(-16px) scale(1.06) !important; }
    #hero-book-text {
      margin: 0 auto; max-width: 480px;
      font-size: 17px; color: var(--text); line-height: 1.55;
      min-height: 3.6em;
      transition: opacity .25s;
    }
    #hero-book-text.is-fading { opacity: 0; }
    .hero-book-text__nb { white-space: nowrap; }
    .hero-book-text__line {
      display: block;
      margin-top: 6px;
      font-size: 15px;
      font-weight: 600;
      color: var(--muted);
      line-height: 1.45;
    }
    .hero__visual-hint {
      font-size: 13px; color: var(--accent); margin-top: 14px; font-weight: 700;
      animation: hint-pulse 2s ease-in-out infinite;
    }
    @keyframes hint-pulse {
      0%, 100% { opacity: 0.65; }
      50% { opacity: 1; }
    }

    .hero__scroll {
      text-align: center; margin-top: 32px;
      font-size: 13px; color: var(--muted);
      animation: scroll-bob 2s ease-in-out infinite;
    }
    .hero__scroll a { color: var(--blue); text-decoration: none; font-weight: 700; }
    @keyframes scroll-bob {
      0%, 100% { transform: translateY(0); opacity: .7; }
      50% { transform: translateY(4px); opacity: 1; }
    }

    /* Premium hero — 2 columns */
    .hero--premium {
      padding: 48px 24px 52px;
    }
    .hero--premium .hero__grid {
      max-width: var(--max);
      margin: 0 auto;
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) minmax(0, 1.05fr);
      column-gap: clamp(36px, 5vw, 72px);
      row-gap: 10px;
      align-items: stretch;
    }
    #chit-main .hero--premium .hero__badge--hero-top {
      grid-column: 1 / -1;
      grid-row: 1;
    }
    .hero--premium .hero__content {
      grid-column: 1;
      grid-row: 2;
      text-align: left;
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      justify-content: center;
      align-self: stretch;
      gap: 0;
      min-height: 0;
    }
    .hero--premium .hero__visual {
      grid-column: 2;
      grid-row: 2;
      display: flex;
      flex-direction: column;
      align-items: stretch;
      justify-content: center;
      align-self: stretch;
      min-height: 0;
    }
  #chit-main .hero--premium .hero__badge,
    #chit-main .hero--premium .hero__badge--hero-top {
      display: inline-flex;
      flex-direction: column;
      justify-self: center;
      align-self: center;
      align-items: center;
      text-align: center;
      margin: 0 0 14px;
      padding: 0 !important;
      width: 100%;
      max-width: none;
      background: transparent !important;
      border: none !important;
      box-shadow: none !important;
      border-radius: 0 !important;
    }
    .hero--premium .hero__badge-line--sub {
      font-size: clamp(17px, 2vw, 22px);
      line-height: 1.4;
      color: var(--muted);
      font-weight: 600;
      letter-spacing: 0.01em;
    }
    .hero--premium .hero__badge-line--title em {
      font-size: clamp(36px, 4.6vw, 46px);
      letter-spacing: 0.05em;
      line-height: 1.1;
      font-weight: 800;
    }
    .hero--premium h1 {
      text-align: left;
      font-size: clamp(28px, 3.3vw, 40px);
      line-height: 1.2;
      margin: 0 0 14px;
      max-width: min(18em, 100%);
      letter-spacing: -0.025em;
      overflow-wrap: break-word;
      word-break: break-word;
    }
    .hero--premium .hero__pleasure {
      display: inline;
      margin: 0;
      font-family: var(--hand), 'Caveat', cursive;
      font-size: 0.92em;
      font-weight: 700;
      font-style: normal;
      line-height: inherit;
      color: var(--accent);
      letter-spacing: 0;
      white-space: nowrap;
    }
    .hero--premium .hero__lead {
      font-size: clamp(15px, 1.6vw, 17px);
      line-height: 1.55;
      color: var(--text-warm);
      margin: 0 0 16px;
      max-width: min(36em, 100%);
      overflow-wrap: break-word;
      word-break: break-word;
    }
    .hero--premium .hero__price {
      margin: 0 0 20px;
      text-align: left;
    }
    .hero__cta {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 10px;
      width: 100%;
      max-width: none;
      margin-bottom: 0;
    }
    .hero--premium .hero__actions {
      display: flex;
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: flex-start;
      align-items: center;
      gap: 12px;
      width: 100%;
    }
    .hero--premium .hero__actions .btn {
      width: auto;
      flex: 0 1 auto;
      white-space: nowrap;
    }
    .hero--premium .hero__actions .btn--outline {
      padding: 14px 22px;
      font-size: 16px;
      border-radius: 999px;
    }
    .hero--premium .hero__cta-note {
      margin-top: 0;
      padding-top: 0;
    }
    .hero--premium .hero__visual-gift {
      margin-top: 14px;
      padding-top: 0;
      align-self: center;
    }
    .hero__cta-note {
      margin: 0;
      font-size: 14px;
      font-weight: 700;
      color: var(--blue-deep);
      line-height: 1.45;
    }
    .hero__visual-gift {
      display: inline-block;
      margin: 0;
      padding: 0;
      font-size: 14px;
      line-height: 1.45;
      font-weight: 700;
      color: var(--accent);
      text-align: center;
      text-decoration: none;
      border-bottom: 1px dashed rgba(143, 125, 163, 0.45);
      background: transparent;
      border-radius: 0;
      transition: color .2s, border-color .2s;
    }
    .hero__visual-gift:hover {
      color: var(--blue-deep);
      border-bottom-color: var(--blue);
      background: transparent;
    }
    .hero--premium .hero__showcase {
      max-width: none;
      width: 100%;
      margin: 0;
      padding: 36px 28px 28px;
    }
    .hero--premium .hero__showcase-label {
      font-size: 11px;
      letter-spacing: 0.04em;
      text-transform: none;
      margin-bottom: 28px;
      color: var(--muted);
      font-weight: 700;
    }
    .hero--premium .book-stack {
      gap: clamp(16px, 3vw, 32px);
      margin-bottom: 24px;
    }
    .hero--premium .book {
      width: clamp(88px, 12vw, 120px);
    }
    .hero--premium .book:nth-child(2) {
      width: clamp(100px, 14vw, 136px);
    }
    .hero--premium .hero__scroll {
      margin-top: 40px;
    }
    @media (max-width: 960px) {
      .hero--premium .hero__grid {
        grid-template-columns: 1fr;
      }
      #chit-main .hero--premium .hero__badge--hero-top {
        grid-column: 1;
        grid-row: 1;
      }
      .hero--premium .hero__content {
        grid-column: 1;
        grid-row: 2;
        align-items: center;
        justify-content: flex-start;
        text-align: center;
        width: 100%;
        max-width: 100%;
        min-width: 0;
      }
      .hero--premium .hero__visual {
        grid-column: 1;
        grid-row: 3;
        justify-content: flex-start;
      }
      #chit-main .hero--premium .hero__badge,
      #chit-main .hero--premium .hero__badge--hero-top {
        align-items: center;
        text-align: center;
      }
      .hero--premium .hero__badge-line--title em {
        font-size: clamp(28px, 7.5vw, 34px);
        letter-spacing: 0.04em;
      }
      .hero--premium h1 { max-width: 100%; text-align: center; font-size: clamp(24px, 7vw, 30px); }
      .hero__cta { align-items: stretch; max-width: 100%; width: 100%; }
      .hero--premium .hero__lead { text-align: center; max-width: 100%; }
      .hero--premium .hero__price { text-align: center; }
      .hero--premium .hero__actions {
        flex-direction: column;
        align-items: stretch;
        justify-content: center;
      }
      .hero--premium .hero__actions .btn { width: 100%; }
      .hero--premium .hero__grid { width: 100%; max-width: 100%; }
      .hero--premium .hero__cta-note,
      .hero--premium .hero__visual-gift {
        margin-top: 12px;
        padding-top: 0;
      }
    }

    .quote-hero {
      max-width: var(--max); margin: 0 auto;
      padding: 40px 24px 48px;
      text-align: center;
      border-top: 1px solid rgba(91,127,166,0.12);
      background: var(--white);
    }
    .quote-hero blockquote {
      margin: 0 auto; max-width: 720px;
      font-family: var(--serif); font-size: 26px; font-style: italic;
      color: var(--text-warm); line-height: 1.45; font-weight: 400;
    }
    @media (max-width: 600px) { .quote-hero blockquote { font-size: 20px; } }
    .quote-hero cite {
      display: block; margin-top: 16px;
      font-family: 'Nunito', sans-serif; font-style: normal;
      font-size: 14px; color: var(--muted); font-weight: 600;
    }

    .quote-marquee {
      overflow: hidden; background: var(--cream-warm);
      border-top: 1px solid rgba(143,125,163,0.2);
      border-bottom: 1px solid rgba(143,125,163,0.2);
      padding: 14px 0;
    }
    .quote-marquee__track {
      display: flex; gap: 48px;
      animation: marquee 40s linear infinite;
      white-space: nowrap; width: max-content;
    }
    .quote-marquee__item {
      font-family: var(--serif); font-style: italic;
      font-size: 15px; color: var(--text-warm); opacity: 0.85;
    }
    .quote-marquee__item span {
      color: var(--accent); font-family: 'Nunito', sans-serif;
      font-style: normal; font-weight: 700; margin-right: 8px;
    }
    @keyframes marquee {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    /* Benefits — закладки на полке */
    .benefits {
      background: linear-gradient(180deg, var(--cream-warm) 0%, var(--white) 55%);
      padding: 32px 24px 44px;
    }
    .benefits__inner {
      max-width: var(--max); margin: 0 auto;
      position: relative;
    }
    .benefits__lead {
      text-align: center;
      font-family: var(--hand);
      font-size: 22px;
      color: var(--accent);
      margin: 0 0 22px;
      letter-spacing: 0.02em;
    }
    .benefits__grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 18px;
      position: relative;
    }
    .benefits__grid::before {
      content: '';
      position: absolute;
      top: 28px; left: 8%; right: 8%;
      border-top: 2px dashed rgba(143,125,163,0.35);
      z-index: 0;
      pointer-events: none;
    }
    @media (max-width: 900px) {
      .benefits__grid { grid-template-columns: repeat(2, 1fr); }
      .benefits__grid::before { display: none; }
    }
    @media (max-width: 520px) {
      .benefits__grid { grid-template-columns: 1fr; gap: 14px; }
    }
    .benefit {
      position: relative;
      z-index: 1;
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 0 0 16px 16px;
      padding: 22px 18px 18px;
      box-shadow: var(--shadow);
      transition: transform .28s ease, box-shadow .28s ease;
      border-top: 5px solid var(--benefit-accent, var(--accent));
    }
    .benefit::after {
      content: '';
      position: absolute; top: -1px; left: 50%;
      transform: translateX(-50%);
      width: 18px; height: 10px;
      background: var(--cream-warm);
      clip-path: polygon(0 0, 100% 0, 50% 100%);
    }
    .benefit:nth-child(1) { --benefit-accent: var(--blue); transform: rotate(-1.8deg); }
    .benefit:nth-child(2) { --benefit-accent: var(--accent-soft); transform: rotate(1.2deg) translateY(8px); }
    .benefit:nth-child(3) { --benefit-accent: var(--blue-light); transform: rotate(-0.8deg) translateY(4px); }
    .benefit:nth-child(4) { --benefit-accent: var(--accent); transform: rotate(1.5deg); }
    .benefit:hover {
      transform: rotate(0deg) translateY(-6px) !important;
      box-shadow: var(--shadow-lg);
    }
    .benefit__tag {
      display: block;
      font-family: var(--hand);
      font-size: 20px;
      color: var(--benefit-accent, var(--accent));
      margin-bottom: 8px;
      line-height: 1;
    }
    .benefit__text {
      margin: 0;
      font-size: 15px; font-weight: 700;
      color: var(--text); line-height: 1.45;
    }

    /* About */
    .about-text {
      max-width: 760px; margin: 0 auto;
      font-size: 18px; line-height: 1.75; color: var(--text);
    }
    .about-text p { margin: 0 0 20px; }
    .about-text strong { color: var(--blue); }
    .pull-quote {
      max-width: 760px; margin: 32px auto 0;
      padding: 28px 32px;
      background: var(--cream-warm);
      border-left: 4px solid var(--accent);
      border-radius: 0 12px 12px 0;
      display: flex;
      align-items: center;
      gap: 28px;
    }
    .pull-quote__body { flex: 1; min-width: 0; }
    .pull-quote__illus {
      flex-shrink: 0;
      width: 112px; height: 112px;
      object-fit: cover;
      border-radius: 50%;
      border: 3px solid rgba(255,255,255,0.85);
      box-shadow: 0 4px 16px rgba(143,125,163,0.15);
      pointer-events: none;
    }
    @media (max-width: 640px) {
      .pull-quote {
        flex-direction: column;
        align-items: flex-start;
        gap: 20px;
        padding: 24px 24px 20px;
      }
      .pull-quote__illus {
        width: 88px; height: 88px;
        align-self: center;
        order: -1;
      }
    }
    .pull-quote p {
      margin: 0;
      font-family: var(--serif); font-size: 20px; font-style: italic;
      color: var(--text-warm); line-height: 1.55;
    }
    .pull-quote cite {
      display: block; margin-top: 12px;
      font-family: 'Nunito', sans-serif; font-style: normal;
      font-size: 14px; color: var(--muted);
    }

    /* Summer timeline */
    .summer-timeline {
      max-width: 760px; margin: 36px auto 0;
      padding: 28px 32px;
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }
    .summer-timeline__track {
      display: flex; align-items: center; justify-content: center;
      gap: 16px; flex-wrap: wrap;
    }
    .summer-timeline__node {
      flex: 1 1 180px; max-width: 220px;
      text-align: center;
      padding: 16px 14px;
      background: var(--blue-pale);
      border-radius: 12px;
      border: 1.5px solid var(--border);
    }
    .summer-timeline__date {
      display: block; font-size: 20px; font-weight: 800;
      color: var(--blue); margin-bottom: 4px;
    }
    .summer-timeline__label {
      display: block; font-size: 14px; color: var(--muted); font-weight: 600;
    }
    .summer-timeline__arrow {
      font-size: 22px; font-weight: 800; color: var(--accent-soft);
      flex-shrink: 0;
    }
    .summer-timeline__total {
      flex: 0 0 100%; text-align: center; margin-top: 4px;
      font-family: var(--hand); font-size: 22px; font-weight: 700;
      color: var(--accent);
    }
    .summer-timeline__node--extra {
      background: var(--accent-pale);
      border-color: rgba(143,125,163,0.35);
    }
    .summer-timeline__node--extra .summer-timeline__date {
      font-size: 17px;
    }
    @media (max-width: 560px) {
      .summer-timeline { padding: 22px 18px; }
      .summer-timeline__arrow { flex: 0 0 100%; transform: rotate(90deg); }
    }

    /* How a lesson works */
    .lesson-steps {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
      gap: 12px 16px;
      max-width: 1100px; margin: 0 auto;
      align-items: start;
    }
    @media (max-width: 900px) {
      .lesson-steps { grid-template-columns: repeat(2, 1fr); gap: 20px; max-width: 420px; }
    }
    @media (max-width: 480px) {
      .lesson-steps { grid-template-columns: 1fr; max-width: 220px; }
    }
    .lesson-step {
      background: transparent;
      border: none;
      border-radius: 0;
      padding: 0;
      text-align: center;
      box-shadow: none;
      transition: transform .2s ease;
    }
    .lesson-step:hover {
      transform: translateY(-6px);
      box-shadow: none;
    }
    .lesson-step__icon {
      width: 100%;
      max-width: 200px;
      margin: 0 auto;
      aspect-ratio: 1;
      display: flex; align-items: center; justify-content: center;
      overflow: visible;
      background: transparent;
      border: none;
      border-radius: 0;
    }
    @media (max-width: 900px) {
      .lesson-step__icon { max-width: 180px; }
    }
    .lesson-step__icon img {
      width: 100%; height: 100%;
      object-fit: contain;
      object-position: center;
      display: block;
    }
    .lesson-steps-note {
      text-align: center; max-width: 560px; margin: 24px auto 0;
      font-size: 15px; color: var(--muted); line-height: 1.55;
    }

    .final-cta-scroller {
      height: max(135vh, 1020px);
      width: 100vw;
      max-width: 100vw;
      margin-left: calc(50% - 50vw);
      margin-right: calc(50% - 50vw);
      position: relative;
      box-sizing: border-box;
    }
    .final-cta {
      position: sticky;
      top: 0;
      height: 100vh;
      min-height: 840px;
      width: 100%;
      text-align: center;
      padding: 48px 8% 64px 24px;
      display: flex; align-items: center; justify-content: flex-end;
      overflow: hidden;
    }
    .final-cta__bg {
      position: absolute; inset: 0; z-index: 0;
      background: url('https://api.chitatelstvo.ru/assets/cta-fairy-tale-bg.png') center bottom / cover no-repeat;
      transform: scale(1.04);
      will-change: transform;
    }
    .final-cta__frame {
      position: relative; z-index: 1;
      max-width: 420px; width: 100%;
      margin-left: auto;
      background: rgba(255, 252, 248, 0.93);
      border: 2px solid rgba(255, 255, 255, 0.85);
      border-radius: 20px;
      padding: 40px 36px 36px;
      box-shadow: 0 16px 48px rgba(26, 42, 74, 0.22);
      backdrop-filter: blur(6px);
      opacity: 0;
      transform: translateX(28px);
      transition: opacity .65s ease, transform .65s ease;
    }
    .final-cta.is-active .final-cta__frame {
      opacity: 1;
      transform: translateX(0);
    }
    @media (max-width: 768px) {
      .final-cta-scroller { height: max(120vh, 860px); }
      .final-cta {
        min-height: 700px;
        padding: 48px 20px 56px;
        justify-content: center;
        align-items: flex-start;
      }
      .final-cta__frame { max-width: 100%; margin-left: 0; }
    }
    @media (max-width: 560px) {
      .final-cta-scroller { height: max(115vh, 780px); }
      .final-cta { min-height: 620px; }
      .final-cta__frame { padding: 28px 24px 24px; }
    }
    @media (prefers-reduced-motion: reduce) {
      .final-cta-scroller { height: auto; }
      .final-cta { position: relative; height: auto; min-height: 840px; }
      .final-cta__bg { transform: none !important; }
      .final-cta__frame { opacity: 1; transform: none; transition: none; }
    }
    .final-cta h2 {
      font-size: 32px; font-weight: 800; color: var(--blue);
      margin: 0 0 12px;
      font-family: var(--serif); font-weight: 600; font-style: italic;
    }
    @media (max-width: 560px) { .final-cta h2 { font-size: 26px; } }
    .final-cta p {
      font-size: 18px; color: var(--muted); max-width: 460px;
      margin: 0 auto 28px; line-height: 1.6;
    }

    /* For whom */
    .for-whom {
      display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px;
      max-width: 800px; margin: 0 auto;
    }
    @media (max-width: 600px) { .for-whom { grid-template-columns: 1fr; } }
    .for-whom__item {
      background: var(--white); border: 1px solid var(--border);
      border-radius: var(--radius); padding: 20px 22px 20px 108px;
      box-shadow: var(--shadow);
      transition: transform .2s, border-color .2s, box-shadow .2s;
      font-size: 16px; line-height: 1.5; color: var(--text);
      position: relative;
    }
    .for-whom__icon-wrap {
      position: absolute; left: 14px; top: 50%;
      transform: translateY(-50%);
      width: 80px; height: 80px;
      border-radius: 14px;
      overflow: hidden;
      flex-shrink: 0;
      background: var(--blue-pale);
    }
    .for-whom__icon {
      width: 100%; height: 100%;
      object-fit: cover;
      object-position: center center;
      display: block;
    }
    .for-whom__icon-wrap--kids .for-whom__icon { object-position: center 42%; }
    .for-whom__icon-wrap--pace .for-whom__icon { object-position: center 38%; }
    .for-whom__icon-wrap--family .for-whom__icon { object-position: center 45%; }
    .for-whom__icon-wrap--parents .for-whom__icon { object-position: center 40%; }
    .for-whom__item:hover {
      transform: translateY(-3px);
      border-color: var(--accent-soft);
      box-shadow: var(--shadow-lg);
    }

    /* Gamification — уровни и бейджи */
    .slovik-meet {
      display: flex;
      align-items: center;
      gap: 20px;
      max-width: 760px;
      margin: 0 auto 44px;
      padding: 8px 4px;
    }
    .slovik-meet__img {
      width: 168px;
      height: 168px;
      flex-shrink: 0;
      object-fit: contain;
      display: block;
    }
    .slovik-meet__copy {
      position: relative;
      min-width: 0;
      flex: 1;
      background: #fff;
      border: 2px solid #C5D9ED;
      border-radius: 20px;
      padding: 22px 26px 20px;
      box-shadow: 0 10px 28px rgba(91, 127, 166, 0.12);
    }
    .slovik-meet__copy::before {
      content: '';
      position: absolute;
      left: -12px;
      top: 50%;
      width: 18px;
      height: 18px;
      background: #fff;
      border-left: 2px solid #C5D9ED;
      border-bottom: 2px solid #C5D9ED;
      transform: translateY(-50%) rotate(45deg);
    }
    .slovik-meet__hi {
      margin: 0 0 10px;
      font-size: 24px;
      font-weight: 800;
      color: var(--blue);
      line-height: 1.3;
    }
    .slovik-meet__hi strong {
      color: var(--accent);
    }
    .slovik-meet__text {
      margin: 0;
      font-size: 17px;
      line-height: 1.55;
      color: var(--muted);
    }
    @media (max-width: 720px) {
      .slovik-meet {
        flex-direction: column;
        text-align: center;
        gap: 14px;
        margin-bottom: 36px;
      }
      .slovik-meet__img {
        width: 132px;
        height: 132px;
      }
      .slovik-meet__copy {
        padding: 18px 18px 16px;
        border-radius: 18px;
      }
      .slovik-meet__copy::before {
        left: 50%;
        top: -10px;
        border-left: 2px solid #C5D9ED;
        border-bottom: none;
        border-top: 2px solid #C5D9ED;
        transform: translateX(-50%) rotate(45deg);
      }
      .slovik-meet__hi { font-size: 20px; }
      .slovik-meet__text { font-size: 16px; }
    }
    .gamify__subtitle {
      text-align: center; font-size: 22px; font-weight: 800;
      color: var(--blue); margin: 0 0 24px;
    }
    .gamify-levels {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
      gap: 20px 12px;
      max-width: 880px; margin: 0 auto 52px;
      position: relative;
    }
    .gamify-levels::before {
      content: '';
      position: absolute; top: 48px; left: 8%; right: 8%;
      height: 2px; z-index: 0;
      background: linear-gradient(90deg, var(--blue-light), var(--accent-soft), var(--accent));
      opacity: 0.35;
    }
    @media (max-width: 720px) {
      .gamify-levels {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        align-items: flex-start;
        gap: 18px 10px;
        max-width: 100%;
        padding: 0 12px;
        box-sizing: border-box;
      }
      .gamify-levels::before { display: none; }
      .gamify-levels__arrow { display: none; }
      .gamify-levels .gamify-item {
        flex: 0 0 calc(33.333% - 10px);
        max-width: 118px;
        min-width: 96px;
        width: auto;
        gap: 8px;
      }
      .gamify-levels .gamify-item__img {
        width: 72px;
        height: 72px;
      }
      .gamify-levels .gamify-item__name {
        font-size: 12px;
        line-height: 1.25;
        min-height: 0;
        max-width: 100%;
        white-space: normal;
        word-break: normal;
        overflow-wrap: normal;
        hyphens: none;
      }
    }
    @media (max-width: 400px) {
      .gamify-levels {
        gap: 16px 8px;
        padding: 0 8px;
      }
      .gamify-levels .gamify-item {
        flex: 0 0 calc(50% - 8px);
        max-width: 150px;
        min-width: 120px;
      }
      .gamify-levels .gamify-item__img {
        width: 68px;
        height: 68px;
      }
      .gamify-levels .gamify-item__name { font-size: 12px; }
    }
    .gamify-item {
      position: relative; z-index: 1;
      display: flex; flex-direction: column; align-items: center;
      text-align: center; gap: 10px;
    }
    .gamify-item__img {
      width: 96px; height: 96px;
      display: flex; align-items: center; justify-content: center;
      flex-shrink: 0;
      background: transparent;
      border: none;
      box-shadow: none;
      border-radius: 0;
      overflow: visible;
    }
    .gamify-item__img img {
      width: 100%; height: 100%;
      object-fit: contain;
      object-position: center;
      display: block;
    }
    .gamify-item__name {
      font-size: 14px; font-weight: 800; color: var(--text);
      line-height: 1.35; max-width: 120px;
    }
    .gamify-levels .gamify-item__name {
      min-height: 2.8em;
    }
    .gamify-item--scale-up .gamify-item__img img {
      transform: scale(1.15);
    }
    .gamify-item--scale-up-sm .gamify-item__img img {
      transform: scale(1.05);
    }
    .gamify-badges {
      display: flex;
      flex-wrap: nowrap;
      justify-content: center;
      align-items: center;
      gap: 32px;
      margin: 0 auto;
    }
    .gamify-badges .gamify-item__img {
      width: 80px; height: 80px;
    }
    @media (max-width: 400px) {
      .gamify-badges { gap: 20px; }
      .gamify-badges .gamify-item__img { width: 68px; height: 68px; }
    }
    .gamify-badges-note {
      text-align: center; max-width: 520px; margin: 20px auto 0;
      font-size: 15px; color: var(--muted); line-height: 1.55;
    }

    /* Tariff cards — как МИФ */
    .tariffs-grid {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px;
      align-items: stretch;
    }
    @media (max-width: 900px) { .tariffs-grid { grid-template-columns: 1fr; max-width: 420px; margin: 0 auto; } }
    .tariff-card {
      background: var(--white); border: 1px solid var(--border);
      border-radius: var(--radius); padding: 28px 24px 24px;
      display: flex; flex-direction: column;
      box-shadow: var(--shadow);
      transition: transform .25s ease, box-shadow .25s ease, border-color .2s;
      transform-origin: center bottom;
    }
    .tariff-card:hover {
      transform: translateY(-8px) rotate(-0.4deg);
      box-shadow: 0 16px 48px rgba(61,82,102,0.14);
      border-color: var(--blue-light);
    }
    .tariff-card--featured:hover { transform: translateY(-10px) rotate(0deg); }
    .tariff-card--featured {
      border-color: var(--blue); position: relative;
      box-shadow: var(--shadow-lg);
    }
    .tariff-card--featured::before {
      content: 'популярный'; position: absolute; top: -12px; left: 50%;
      transform: translateX(-50%);
      background: var(--accent); color: #fff;
      font-size: 11px; font-weight: 800; padding: 4px 14px;
      border-radius: 999px; white-space: nowrap;
      letter-spacing: 0.03em;
    }
    .tariff-card__mood {
      font-family: var(--serif); font-style: italic;
      font-size: 14px; color: var(--muted);
      margin: -8px 0 16px; line-height: 1.4;
    }
    .tariff-card__name {
      font-size: 22px; font-weight: 800; color: var(--blue);
      margin: 0 0 16px;
    }
    .tariff-card__list {
      list-style: none; padding: 0; margin: 0 0 24px; flex: 1;
    }
    .tariff-card__list li {
      position: relative; padding-left: 22px; margin-bottom: 10px;
      font-size: 15px; color: var(--text); line-height: 1.45;
    }
    .tariff-card__list li::before {
      content: '•'; position: absolute; left: 0;
      color: var(--blue-light); font-weight: 800;
    }
    .tariff-card__price {
      font-size: 32px; font-weight: 800; color: var(--text);
      margin-bottom: 4px;
    }
    .tariff-card__note {
      font-size: 13px; color: var(--muted); margin-bottom: 20px;
    }
    .tariff-card__meet {
      font-size: 13px; color: var(--muted); line-height: 1.45;
      padding: 10px 12px;
      background: var(--cream);
      border-radius: 10px;
    }
    .tariff-card__meet-slot {
      margin: -12px 0 20px;
      min-height: 72px;
    }
    .tariff-card__meet-slot[aria-hidden="true"] {
      visibility: hidden;
    }
    .tariff-card__meet-line { display: block; }
    .tariff-card__meet-line--muted { font-size: 12px; }

    /* Programs accordion */
    .prog-block { margin-bottom: 40px; }
    .prog-shelf {
      height: auto; min-height: 120px;
      margin: 0 auto 28px; max-width: 900px;
      border-radius: 12px; overflow: hidden;
    }
    .prog-shelf img {
      display: block; width: 100%; height: auto;
    }
    .prog-block__title {
      font-size: 22px; font-weight: 800; color: var(--blue);
      margin: 0 0 6px;
    }
    .prog-block__sub { font-size: 15px; color: var(--muted); margin: 0 0 8px; }
    .prog-block__sub:last-child { margin-bottom: 16px; }
    .prog-block__note {
      max-width: 680px; margin: 0 0 20px;
      font-size: 16px; line-height: 1.55; color: var(--text);
    }
    .prog-course-link {
      margin: 0 0 20px;
    }
    .prog-course-link a {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 10px 18px;
      border-radius: 999px;
      background: var(--white);
      border: 2px solid var(--border);
      color: var(--blue);
      font-size: 14px;
      font-weight: 800;
      text-decoration: none;
      transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s;
    }
    .prog-course-link a:hover {
      border-color: var(--blue-light);
      box-shadow: var(--shadow);
      transform: translateY(-1px);
    }
    .acc { display: flex; flex-direction: column; gap: 8px; }
    .acc-item {
      background: var(--white); border: 1px solid var(--border);
      border-radius: 12px; overflow: hidden;
    }
    .acc-head {
      width: 100%; text-align: left; background: var(--white); border: none;
      padding: 18px 22px; font-family: inherit; font-size: 16px; font-weight: 700;
      color: var(--blue); cursor: pointer;
      display: flex; justify-content: space-between; align-items: center;
    }
    .acc-head:hover { background: var(--cream); }
    .acc-head.is-open {
      background: var(--white);
      border-bottom: 1px solid var(--border);
    }
    .acc-head::after { content: '+'; font-size: 22px; color: var(--blue-light); font-weight: 400; }
    .acc-head.is-open::after { content: '−'; color: var(--blue); }
    .acc-body { display: none; padding: 20px 22px 24px; background: #FAFBFC; }
    .acc-body.is-open { display: block; }
    .acc-body p { margin: 0 0 20px; font-size: 15px; color: var(--muted); line-height: 1.55; }

    .prog-period {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 16px 18px;
      margin-bottom: 12px;
    }
    .prog-period:last-child { margin-bottom: 0; }
    .prog-period__title {
      font-size: 15px; font-weight: 800; color: var(--blue);
      margin: 0 0 12px;
    }
    .prog-tales {
      list-style: none; margin: 0; padding: 0;
    }
    .prog-tales li {
      display: flex; align-items: baseline; gap: 12px;
      padding: 10px 0;
      border-top: 1px solid #F0F2F5;
      font-size: 15px; color: var(--text); line-height: 1.45;
    }
    .prog-tales li:first-child { border-top: none; padding-top: 0; }
    .prog-tales li:last-child { padding-bottom: 0; }
    .prog-week {
      flex-shrink: 0;
      width: 28px; height: 28px;
      background: var(--blue-pale); color: var(--blue);
      border-radius: 8px;
      font-size: 13px; font-weight: 800;
      display: flex; align-items: center; justify-content: center;
    }
    .prog-tale-title { flex: 1; }

    .tale-cards {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
    }
    @media (max-width: 640px) { .tale-cards { grid-template-columns: 1fr; } }
    .tale-card {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 16px 18px;
      transition: transform .25s, box-shadow .25s, border-color .25s;
      cursor: pointer;
      position: relative;
    }
    .tale-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow);
      border-color: var(--blue-light);
    }
    .tale-card.is-open {
      border-color: var(--accent);
      box-shadow: 0 8px 28px rgba(143,125,163,0.16);
    }
    .tale-card__action {
      display: none; margin-top: 12px; padding-top: 12px;
      border-top: 1px solid var(--border);
      font-size: 13px; font-weight: 700; color: var(--accent);
    }
    .tale-card__action a {
      color: var(--accent); text-decoration: none; font-weight: 700;
    }
    .tale-card__action a:hover { text-decoration: underline; }
    .tale-card__week {
      font-size: 11px; font-weight: 800; letter-spacing: 0.06em;
      text-transform: uppercase; color: var(--accent); margin-bottom: 6px;
    }
    .tale-card__title {
      font-size: 16px; font-weight: 800; color: var(--blue);
      margin: 0 0 8px; line-height: 1.3;
    }
    .tale-card__dates { margin: 0 0 8px; }
    .tale-card__date {
      font-size: 13px; color: var(--muted); font-weight: 600;
    }
    .tale-card__date--open {
      color: var(--blue-deep);
    }
    .tale-card__desc {
      font-size: 14px; color: var(--muted); line-height: 1.5; margin: 0 0 8px;
    }
    .tale-card__quote {
      font-family: var(--serif); font-style: italic;
      font-size: 13px; color: var(--text-warm); opacity: 0.8;
      margin: 0; padding-top: 8px;
      border-top: 1px dashed var(--border);
    }

    /* Enrollment panel */
    .enroll-panel {
      background: var(--white); border: 1px solid var(--border);
      border-radius: 20px; box-shadow: var(--shadow-lg);
      padding: 40px 36px; max-width: 880px; margin: 0 auto;
    }
    @media (max-width: 600px) { .enroll-panel { padding: 28px 20px; } }
    .enroll-panel__title {
      font-size: 28px; font-weight: 800; color: var(--blue);
      margin: 0 0 8px; text-align: center;
    }
    .enroll-panel__lead {
      text-align: center; color: var(--muted); margin: 0 0 28px; font-size: 16px;
    }
    .enroll-course-lock {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 16px;
      margin: 0 0 28px;
      padding: 18px 20px;
      border-radius: 16px;
      border: 1px solid var(--border);
      background: linear-gradient(135deg, #fff 0%, #F0F5FA 100%);
    }
    .enroll-course-lock[hidden] { display: none !important; }
    .enroll-course-lock__label {
      display: block;
      font-size: 13px;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 4px;
    }
    .enroll-course-lock__title {
      display: block;
      font-size: 22px;
      font-weight: 800;
      color: var(--text);
      line-height: 1.25;
    }
    .enroll-course-lock__meta {
      display: block;
      margin-top: 4px;
      font-size: 15px;
      color: var(--muted);
    }
    .enroll-course-lock__change {
      flex-shrink: 0;
      border: none;
      background: transparent;
      color: var(--blue);
      font: inherit;
      font-size: 14px;
      font-weight: 800;
      cursor: pointer;
      padding: 8px 0;
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .enroll-course-lock__change:hover { color: var(--accent); }
    #chit-main.is-course-locked #chit-step-group { display: none; }
    @media (max-width: 600px) {
      .enroll-course-lock { flex-direction: column; gap: 10px; padding: 16px; }
      .enroll-course-lock__title { font-size: 19px; }
    }

    /* Бесплатные пробные уроки — отдельный вход, не путать с тарифом «Разовое» */
    .enroll-trial {
      background: linear-gradient(165deg, #F3EEF7 0%, #EEF4FA 100%);
      border: 1px solid #D9CFE6;
      border-radius: 16px;
      padding: 22px 20px 20px;
      margin: 0 0 28px;
    }
    .enroll-trial__head { text-align: center; margin-bottom: 16px; }
    .enroll-trial__badge {
      display: inline-block;
      font-size: 12px; font-weight: 800; letter-spacing: 0.04em;
      text-transform: uppercase; color: #6B5A82;
      background: rgba(255,255,255,0.75); border-radius: 999px;
      padding: 5px 12px; margin-bottom: 10px;
    }
    .enroll-trial__title {
      margin: 0 0 6px; font-size: 22px; font-weight: 800; color: var(--blue);
    }
    .enroll-trial__sub {
      margin: 0; font-size: 15px; line-height: 1.45; color: var(--muted);
    }
    .enroll-trial__cards {
      display: grid; grid-template-columns: 1fr 1fr; gap: 12px;
    }
    @media (max-width: 640px) {
      .enroll-trial__cards { grid-template-columns: 1fr; }
    }
    .enroll-trial__card--early {
      border-color: #D4C4A8;
      background: linear-gradient(180deg, #FFFBF5 0%, #fff 70%);
    }
    .enroll-trial__card {
      display: flex; flex-direction: column; gap: 4px;
      text-decoration: none; color: inherit;
      background: var(--white); border: 1.5px solid var(--border);
      border-radius: 14px; padding: 16px 16px 14px;
      transition: border-color .15s, transform .15s, box-shadow .15s;
    }
    .enroll-trial__card:hover {
      border-color: var(--blue-light);
      box-shadow: var(--shadow);
      transform: translateY(-2px);
    }
    .enroll-trial__age {
      font-size: 12px; font-weight: 800; letter-spacing: 0.04em;
      text-transform: uppercase; color: var(--accent);
    }
    .enroll-trial__name {
      font-size: 17px; font-weight: 800; color: var(--blue); line-height: 1.3;
    }
    .enroll-trial__author {
      font-size: 13px; color: var(--muted); margin-bottom: 6px;
    }
    .enroll-trial__cta {
      margin-top: auto; padding-top: 8px;
      font-size: 14px; font-weight: 800; color: var(--blue);
    }
    .enroll-or {
      display: flex; align-items: center; gap: 14px;
      margin: 0 0 28px; color: var(--muted); font-size: 13px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.06em;
    }
    .enroll-or::before,
    .enroll-or::after {
      content: ''; flex: 1; height: 1px; background: var(--border);
    }
    .step-hint--always { display: block; }
    .block-preview__note {
      font-size: 13px; color: var(--muted); margin: -4px 0 10px;
    }
    .step-label {
      font-size: 14px; letter-spacing: 0.02em;
      color: var(--muted); font-weight: 600; margin-bottom: 14px;
    }
    .step-label em {
      font-style: normal; color: var(--accent); font-weight: 800;
    }
    .step-block { margin-bottom: 32px; }
    .step-block--choice { margin-bottom: 24px; }
    .step-block--choice .step-label { margin-bottom: 10px; }
    .step-hint {
      margin: -6px 0 14px;
      font-size: 14px;
      line-height: 1.5;
      color: var(--muted);
    }
    .group-title { font-size: 17px; font-weight: 800; color: var(--blue); margin: 0 0 12px; }
    .group-block { margin-bottom: 20px; }
    .pills { display: flex; flex-wrap: wrap; gap: 8px; }
    .pill {
      border: 1.5px solid var(--border); background: var(--white);
      color: var(--blue); border-radius: 999px;
      padding: 10px 18px; font-size: 14px; font-weight: 700;
      cursor: pointer; font-family: inherit; transition: all .15s;
    }
    .pill:hover { border-color: var(--blue-light); background: var(--blue-pale); }
    .pill.is-active { background: var(--blue); border-color: var(--blue); color: #fff; }
    .pill.is-disabled,
    .pill:disabled {
      opacity: 0.45;
      cursor: not-allowed;
    }

    /* Шаг 1 записи: три дорожки вместо россыпи пилюль */
    .enroll-tracks {
      display: grid;
      gap: 14px;
    }
    .enroll-track {
      padding: 18px 18px 16px;
      border-radius: 18px;
      border: 1px solid var(--border);
      background: var(--white);
      box-shadow: 0 1px 0 rgba(61, 82, 102, 0.04);
    }
    .enroll-track__head { margin-bottom: 14px; }
    .enroll-track__title {
      margin: 0 0 4px;
      font-size: 18px;
      font-weight: 800;
      color: var(--text);
      line-height: 1.25;
    }
    .enroll-track__sub {
      margin: 0;
      font-size: 14px;
      line-height: 1.45;
      color: var(--muted);
    }
    .enroll-track__opts.pills {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }
    #chit-groups-basic.enroll-track__opts {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
    .enroll-opt.pill {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 2px;
      width: 100%;
      margin: 0;
      border-radius: 14px;
      padding: 14px 14px 12px;
      text-align: left;
      min-height: 64px;
    }
    .enroll-opt__name {
      font-size: 15px;
      font-weight: 800;
      line-height: 1.25;
      color: inherit;
    }
    .enroll-opt__meta {
      font-size: 13px;
      font-weight: 600;
      opacity: 0.72;
      color: inherit;
    }
    .enroll-opt.pill.is-active {
      box-shadow: 0 6px 18px rgba(91, 127, 166, 0.22);
    }
    @media (max-width: 720px) {
      .enroll-track__opts.pills,
      #chit-groups-basic.enroll-track__opts {
        grid-template-columns: 1fr 1fr;
      }
    }
    @media (max-width: 420px) {
      .enroll-track { padding: 16px 14px 14px; }
      .enroll-track__opts.pills,
      #chit-groups-basic.enroll-track__opts {
        grid-template-columns: 1fr;
      }
    }
    /* Старт 15 июля скрыт для тарифа «С преподавателем» (и через класс, и через :has — без JS) */
    #chit-main.is-with-teacher-stage1-closed #chit-stages [data-stage="1"],
    #chit-main.is-cohort-course #chit-stages [data-stage="2"],
    #chit-main.is-early-course #chit-stages [data-stage="2"],
    #chit-main:has(#chit-tariffs .pick-card[data-tariff="with_teacher"].is-active:not(.is-disabled)) #chit-stages [data-stage="1"] {
      display: none !important;
      visibility: hidden !important;
      pointer-events: none !important;
      position: absolute !important;
      width: 0 !important;
      height: 0 !important;
      overflow: hidden !important;
      margin: 0 !important;
      padding: 0 !important;
      border: 0 !important;
    }
    .tariff-pick {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px;
    }
    @media (max-width: 700px) { .tariff-pick { grid-template-columns: 1fr; } }
    .pick-card {
      border: 2px solid var(--border); border-radius: 14px;
      padding: 20px 16px; cursor: pointer; text-align: center;
      transition: transform .2s, box-shadow .2s, border-color .2s; background: var(--white);
    }
    .pick-card:hover { border-color: var(--blue-light); transform: translateY(-4px); box-shadow: var(--shadow); }
    .pick-card.is-active { border-color: var(--blue); background: var(--blue-pale); }
    .pick-card.is-disabled {
      opacity: 0.45;
      pointer-events: none;
      cursor: not-allowed;
      transform: none;
      box-shadow: none;
    }
    .pick-card.is-disabled:hover { border-color: var(--border); transform: none; box-shadow: none; }
    .pick-card__tag { font-size: 11px; font-weight: 800; color: var(--accent); text-transform: uppercase; letter-spacing: .06em; }
    .pick-card__name { font-size: 16px; font-weight: 800; color: var(--blue); margin: 6px 0; }
    .pick-card__price { font-size: 20px; font-weight: 800; color: var(--text); }
    .pick-card__hint {
      font-size: 12px; color: var(--muted); line-height: 1.35; margin-top: 8px;
    }
    .date-box {
      display: none; background: var(--cream);
      border-radius: 14px; padding: 22px; border: 1px solid var(--border);
    }
    .date-box.is-visible { display: block; }
    .stage-row { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 16px; }
    .tales { display: grid; gap: 8px; }
    .tales-prompt {
      grid-column: 1 / -1;
      margin: 8px 0 4px;
      font-size: 15px;
      font-weight: 700;
      color: var(--blue);
    }
    .block-preview__banner {
      background: #E8F2EA;
      color: #2F5D3A;
      border: 1px solid #C5DFC9;
      border-radius: 10px;
      padding: 12px 14px;
      font-size: 15px;
      font-weight: 700;
      margin-bottom: 12px;
      line-height: 1.4;
    }
    .block-preview__next {
      margin: 14px 0 0;
      font-size: 15px;
      font-weight: 700;
      color: var(--accent);
    }
    .tale-btn {
      text-align: left; border: 1.5px solid var(--border); background: var(--white);
      border-radius: 10px; padding: 12px 14px; cursor: pointer;
      font-size: 14px; color: var(--text); font-family: inherit; width: 100%;
      transition: all .15s;
    }
    .tale-btn:hover { border-color: var(--blue-light); }
    .tale-btn.is-active { border-color: var(--blue); background: var(--accent-pale); }
    .tale-num {
      display: inline-block; width: 22px; height: 22px;
      background: var(--blue-pale); border-radius: 6px;
      text-align: center; line-height: 22px; font-size: 12px;
      font-weight: 800; margin-right: 8px; color: var(--blue);
    }
    .tale-btn.is-active .tale-num { background: var(--blue); color: #fff; }
    .tale-btn__body { display: block; margin-top: 2px; }
    .tale-btn__title { display: block; font-weight: 700; color: var(--text); }
    .tale-schedule {
      display: block; margin-top: 6px;
      font-size: 13px; color: var(--muted); line-height: 1.45;
    }
    .tale-schedule__meet,
    .tale-schedule__line { display: block; }
    .tale-schedule__line { margin-top: 3px; }
    .tale-schedule__meet--optional { color: #5a4d8c; }
    .tale-schedule__meet--online { color: var(--blue-deep); }
    .block-preview { font-size: 14px; color: var(--blue); line-height: 1.6; margin-top: 14px; }
    .block-preview__title {
      margin-bottom: 12px; font-size: 15px; font-weight: 700; color: var(--blue);
    }
    .block-preview__cards { display: grid; gap: 10px; }
    .block-preview__card {
      background: #fff;
      border: 1.5px solid #C5D9ED;
      border-radius: 12px;
      padding: 12px 14px;
      color: var(--blue);
    }
    .block-preview__card .tale-schedule,
    .block-preview__card .tale-schedule__line {
      color: var(--muted);
    }
    .block-preview__num { font-size: 12px; font-weight: 800; color: var(--accent); }
    .block-preview__name { font-size: 15px; font-weight: 700; color: var(--blue); margin: 2px 0 4px; }
    .summary {
      background: var(--accent-pale); border-radius: 12px;
      padding: 18px 24px; font-size: 22px; min-height: 56px;
      border: 1.5px dashed var(--accent);
      font-family: var(--hand); font-weight: 600;
      line-height: 1.45;
      color: var(--text-warm);
    }
    .summary.is-empty {
      display: flex; align-items: center; justify-content: center;
      text-align: center;
    }
    .summary strong { color: var(--blue); font-weight: 700; }
    .summary-empty { color: var(--muted); font-family: 'Nunito', sans-serif; font-size: 15px; font-weight: 600; line-height: 1.45; }

    /* Form */
    .form-section {
      margin-top: 32px; padding-top: 32px;
      border-top: 1px solid var(--border);
    }
    .form-section h3 {
      font-size: 22px; font-weight: 800; color: var(--blue);
      margin: 0 0 20px; text-align: center;
    }
    .enroll-checkout {
      margin-top: 32px; padding-top: 32px;
      border-top: 1px solid var(--border);
    }
    .enroll-checkout h3 {
      font-size: 22px; font-weight: 800; color: var(--blue);
      margin: 0 0 12px; text-align: center;
    }
    .enroll-checkout .form-grid {
      margin-bottom: 24px;
    }
    .enroll-checkout .btn--pay {
      margin-top: 4px;
    }
    .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    @media (max-width: 600px) { .form-grid { grid-template-columns: 1fr; } }
    .form-field label {
      display: block; font-size: 13px; font-weight: 700;
      color: var(--blue); margin-bottom: 6px;
    }
    .form-field input, .form-field select {
      width: 100%; padding: 13px 16px;
      border: 1.5px solid var(--border); border-radius: 10px;
      font-family: inherit; font-size: 16px; color: var(--text);
      background: var(--white);
    }
    .form-field input:focus, .form-field select:focus {
      outline: none; border-color: var(--blue-light);
      box-shadow: 0 0 0 3px rgba(136,169,209,0.25);
    }
    .form-field--full { grid-column: 1 / -1; }
    .form-intro {
      text-align: center; max-width: 560px; margin: 0 auto 24px;
      font-size: 15px; line-height: 1.6; color: var(--muted);
    }
    .form-consent {
      text-align: center; margin-top: 14px;
      font-size: 12px; line-height: 1.5; color: var(--muted);
      max-width: 480px; margin-left: auto; margin-right: auto;
    }
    .form-consent a { color: var(--blue); }
    .st100-warning,
    #chit-st100-warning {
      display: none !important;
      visibility: hidden !important;
      height: 0 !important;
      margin: 0 !important;
      padding: 0 !important;
      overflow: hidden !important;
      border: 0 !important;
    }
    .st100-warning strong { color: #5a4a38; }

    /* FAQ — письма */
    .faq-list { max-width: 760px; margin: 0 auto; }
    .faq-item {
      border-bottom: 1px solid var(--border);
      background: var(--white);
      margin-bottom: 12px;
      border-radius: 12px;
      border: 1px solid var(--border);
      overflow: hidden;
    }
    .faq-item:last-child { margin-bottom: 0; }
    .faq-q {
      width: 100%; text-align: left; background: #FAFBFC; border: none;
      padding: 18px 22px; font-family: inherit;
      cursor: pointer; display: block;
    }
    .faq-q__from {
      display: block; font-size: 11px; font-weight: 700;
      letter-spacing: 0.04em; color: var(--accent);
      margin-bottom: 6px; text-transform: lowercase;
    }
    .faq-q__text {
      display: flex; justify-content: space-between; align-items: center;
      font-size: 17px; font-weight: 700; color: var(--blue);
    }
    .faq-q__text::after { content: '+'; font-size: 22px; color: var(--accent); font-weight: 400; flex-shrink: 0; margin-left: 12px; }
    .faq-q.is-open .faq-q__text::after { content: '−'; }
    .faq-a {
      display: none; padding: 0 22px 20px;
      color: var(--muted); font-size: 16px; line-height: 1.65; margin: 0;
      background: var(--white);
    }
    .faq-a.is-open { display: block; }
    .faq-a__sign {
      display: block; font-size: 12px; font-weight: 700;
      color: var(--blue); margin-bottom: 8px;
    }

    /* Единый паттерн луга: основатели + подвал (без шва между секциями) */
    .chit-meadow-bg {
      position: relative;
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
      background-color: var(--cream);
      background-image: url('https://api.chitatelstvo.ru/assets/pattern-meadow-books.png');
      background-repeat: repeat;
      background-position: center top;
      background-size: 420px;
    }
    .chit-meadow-bg::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(
        180deg,
        rgba(255,255,255,0.84) 0%,
        rgba(246,244,249,0.76) 42%,
        rgba(246,244,249,0.78) 100%
      );
      pointer-events: none;
      z-index: 0;
    }
    .chit-meadow-bg .founder,
    .chit-meadow-bg .footer {
      background-image: none !important;
      background-color: transparent;
      position: relative;
      z-index: 1;
    }
    .chit-meadow-bg .founder::before,
    .chit-meadow-bg .footer::before {
      display: none;
    }

    /* Декоративный паттерн — полоска и подвал */
    .pattern-strip {
      position: relative;
      height: 104px;
      background-color: var(--cream);
      background-image: url('https://api.chitatelstvo.ru/assets/pattern-meadow-books.png');
      background-repeat: repeat;
      background-position: center;
      background-size: 380px;
      border-top: 1px solid rgba(212,226,239,0.45);
    }
    .pattern-strip::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(
        180deg,
        var(--cream) 0%,
        rgba(246,244,249,0.58) 18%,
        rgba(246,244,249,0.58) 82%,
        #FAFBFC 100%
      );
      pointer-events: none;
    }
    @media (max-width: 600px) {
      .pattern-strip { height: 72px; background-size: 280px; }
    }

    .footer {
      position: relative;
      background-color: var(--cream);
      background-image: url('https://api.chitatelstvo.ru/assets/pattern-meadow-books.png');
      background-repeat: repeat;
      background-position: center top;
      background-attachment: scroll;
      background-size: 420px;
      padding: 48px 24px 56px;
      text-align: center; font-size: 14px; color: var(--muted);
    }
    .footer::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(
        180deg,
        rgba(255,255,255,0.9) 0%,
        rgba(246,244,249,0.84) 100%
      );
      pointer-events: none;
    }
    .footer__inner {
      position: relative;
      z-index: 1;
    }
    .footer__logo {
      width: 256px;
      height: auto;
      display: block;
      margin: 0 auto 20px;
    }
    .footer__warm {
      font-family: var(--serif); font-style: italic;
      font-size: 18px; color: var(--text-warm);
      margin: 0 0 8px;
    }
    .footer__copy { margin: 0; font-size: 13px; }
    .footer__legal {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px 16px;
      margin: 0 0 12px;
      font-family: var(--sans);
      font-size: 13px;
    }
    .footer__legal a {
      color: var(--blue);
      text-decoration: none;
    }
    .footer__legal a:hover { text-decoration: underline; }
    .footer__contact {
      margin: 0 0 8px;
      font-family: var(--sans);
      font-size: 13px;
    }
    .footer__contact a { color: var(--blue); text-decoration: none; }
    .footer__contact a:hover { text-decoration: underline; }
    .footer__seller {
      margin: 0;
      font-family: var(--sans);
      font-size: 12px;
      color: var(--muted);
    }

    /* Founder */
    .founder {
      position: relative;
      padding: 40px 24px 56px;
      margin-top: 0;
      background-color: var(--cream-warm);
      background-image: url('https://api.chitatelstvo.ru/assets/pattern-meadow-books.png');
      background-repeat: repeat;
      background-position: center top;
      background-size: 420px;
    }
    .founder::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(
        180deg,
        rgba(255,255,255,0.84) 0%,
        rgba(246,244,249,0.76) 50%,
        rgba(255,255,255,0.82) 100%
      );
      pointer-events: none;
    }
    .founder .section__inner { position: relative; z-index: 1; }
    .founder .section__title { margin-bottom: 32px; }
    .founder__grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 24px;
      max-width: 1080px;
      margin: 0 auto;
    }
    .founder__card {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px;
      background: var(--white);
      border: 2px solid var(--accent-soft);
      border-radius: 24px;
      padding: 32px 28px 36px;
      box-shadow: 0 16px 48px rgba(143,125,163,0.18);
      text-align: center;
    }
    .founder__photo-wrap {
      flex-shrink: 0;
      width: 200px;
      height: 240px;
      border-radius: 20px;
      overflow: hidden;
      border: 4px solid var(--accent-pale);
      box-shadow: 0 8px 28px rgba(91,127,166,0.22);
      background: #3d4a56;
    }
    .founder__photo {
      display: block;
      width: 100%;
      height: 100%;
      max-width: none;
      aspect-ratio: auto;
      object-fit: cover;
      object-position: center 20%;
      border: none;
      border-radius: 0;
      box-shadow: none;
    }
    .founder__photo--olga {
      transform: scale(1.14) translateY(-5%);
      transform-origin: center 32%;
      object-position: center 42%;
    }
    .founder__photo--anastasia {
      transform: scale(1.32) translateY(-1%);
      transform-origin: center 30%;
      object-position: center 18%;
    }
    .founder__body { flex: 1; min-width: 0; width: 100%; }
    .founder__name {
      font-size: 26px; font-weight: 800; color: var(--blue);
      margin: 0 0 6px; line-height: 1.2;
      letter-spacing: -0.02em;
    }
    .founder__role {
      margin: 0 0 14px;
      font-size: 13px;
      font-weight: 800;
      color: var(--accent);
      text-transform: uppercase;
      letter-spacing: .05em;
    }
    .founder__bio {
      margin: 0; font-size: 16px; line-height: 1.6; color: var(--text);
      text-align: left;
    }
    .founder__bio strong { color: var(--accent); font-weight: 800; }
    @media (max-width: 900px) {
      .founder__grid {
        grid-template-columns: 1fr;
        max-width: 520px;
      }
    }
    @media (max-width: 680px) {
      .founder__card { padding: 28px 22px 32px; gap: 18px; }
      .founder__photo-wrap { width: 180px; height: 220px; }
      .founder__name { font-size: 24px; }
      .founder__bio { font-size: 15px; }
    }
    .quote-marquee:hover .quote-marquee__track { animation-play-state: paused; }

    .quote-hero { cursor: pointer; user-select: none; }
    .quote-hero blockquote { transition: opacity .35s; }
    .quote-hero.is-fading blockquote { opacity: 0; }
    .quote-hero__hint {
      display: block; margin-top: 20px;
      font-size: 12px; color: var(--accent); font-weight: 700;
      opacity: 0.8;
    }

    /* В Zero Block секции видны сразу — opacity:0 ломает страницу */
    .reveal {
      transform: translateY(20px);
      transition: transform .6s ease, opacity .6s ease;
    }
    .reveal.is-visible { transform: none; }


    .chit-hidden { position: absolute; left: -9999px; opacity: 0; height: 0; overflow: hidden; }

    /* Feedback tab */
    .feedback-tab {
      position: fixed;
      bottom: 0; right: 24px;
      z-index: 200;
      font-family: 'Nunito', sans-serif;
    }
    .feedback-tab__toggle {
      display: flex; align-items: center; gap: 8px;
      background: var(--accent); color: #fff;
      border: none; cursor: pointer;
      border-radius: 14px 14px 0 0;
      padding: 12px 20px 14px;
      font-size: 14px; font-weight: 800;
      font-family: inherit;
      box-shadow: 0 -4px 24px rgba(143, 125, 163, 0.35);
      transition: background .2s, transform .15s;
    }
    .feedback-tab__toggle:hover { background: #7a6a8f; transform: translateY(-2px); }
    .feedback-tab__toggle-icon {
      font-size: 18px; line-height: 1;
    }
    .feedback-tab__panel {
      position: absolute;
      bottom: calc(100% - 1px);
      right: 0;
      width: 300px;
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 16px 16px 0 16px;
      padding: 22px 22px 20px;
      box-shadow: 0 -8px 32px rgba(61, 82, 102, 0.14);
      opacity: 0;
      transform: translateY(8px);
      pointer-events: none;
      transition: opacity .25s ease, transform .25s ease;
    }
    .feedback-tab.is-open .feedback-tab__panel {
      opacity: 1;
      transform: translateY(0);
      pointer-events: auto;
    }
    .feedback-tab__title {
      margin: 0 0 6px;
      font-size: 17px; font-weight: 800; color: var(--blue);
    }
    .feedback-tab__text {
      margin: 0 0 16px;
      font-size: 14px; line-height: 1.5; color: var(--muted);
    }
    .feedback-tab__links {
      display: flex; flex-direction: column; gap: 10px;
    }
    .feedback-tab__links a {
      font-size: 14px; font-weight: 700; color: var(--blue);
      text-decoration: none;
      padding: 10px 14px;
      background: var(--blue-pale);
      border-radius: 10px;
      transition: background .15s;
    }
    .feedback-tab__links a:hover { background: var(--accent-pale); color: var(--accent); }
    @media (max-width: 720px) {
      .feedback-tab {
        left: 50%;
        right: 0;
        z-index: 200;
      }
      .feedback-tab__toggle {
        width: 100%;
        justify-content: center;
        border-radius: 0;
        box-shadow: none;
        border-left: 1px solid rgba(255, 255, 255, 0.28);
        padding: 12px 10px max(14px, env(safe-area-inset-bottom));
        font-size: 13px;
        line-height: 1.2;
        white-space: nowrap;
      }
      .feedback-tab__panel {
        width: min(100vw - 16px, 320px);
        right: 8px;
        left: auto;
        border-radius: 16px 16px 0 16px;
      }
    }
    @media (max-width: 380px) {
      .feedback-tab__toggle { font-size: 12px; }
    }

    /* Tilda: фон страницы, без горизонтального скролла */
    html,
    body,
    .t-body,
    #allrecords,
    #allrecords .t-records,
    #allrecords .t-rec {
      background-color: #F6F4F9;
      overflow-x: hidden !important;
      max-width: 100%;
    }
    .t396,
    .t396__artboard,
    .t396__carrier,
    .t396__filter,
    .tn-elem[data-elem-type="html"] {
      transform: none !important;
      zoom: 1 !important;
      left: 0 !important;
      width: 100% !important;
      max-width: 100% !important;
      min-width: 0 !important;
    }
    #allrecords #chit-main a { color: inherit; text-decoration: none; }
    #allrecords #chit-main a.btn,
    #allrecords #chit-main button.btn,
    #allrecords #chit-main .btn {
      color: #fff !important;
      -webkit-text-fill-color: #fff !important;
    }
    #allrecords #chit-main a.btn--outline,
    #allrecords #chit-main .btn--outline,
    #allrecords #chit-main a.btn.btn--outline {
      color: var(--blue) !important;
      -webkit-text-fill-color: var(--blue) !important;
      background: transparent !important;
      border: 2px solid var(--blue-light) !important;
    }
    #allrecords #chit-main .pill.is-active {
      color: #fff !important;
      -webkit-text-fill-color: #fff !important;
    }
    #allrecords #chit-main .site-nav a { color: var(--muted) !important; }
    #allrecords #chit-main .site-nav a:hover { color: var(--blue) !important; }
    #allrecords #chit-main .header-cta { color: #fff !important; }
    #allrecords #chit-main .btn,
    #allrecords #chit-main a.btn,
    #allrecords #chit-main .btn--pay { color: #fff !important; }
    #allrecords #chit-main .btn--outline,
    #allrecords #chit-main a.btn.btn--outline {
      color: var(--blue) !important;
      -webkit-text-fill-color: var(--blue) !important;
      background: transparent !important;
      border: 2px solid var(--blue-light) !important;
    }
    #allrecords #chit-main .pill.is-active { color: #fff !important; }
    #allrecords #chit-main .faq-a { display: none; }
    #allrecords #chit-main .faq-a.is-open { display: block; }

    /* ── Мобильная версия ── */
    @media (max-width: 960px) {
      #chit-main {
        font-size: 16px;
        -webkit-text-size-adjust: 100%;
        text-size-adjust: 100%;
      }

      /* full-bleed убран глобально — здесь только типографика и отступы */
      .section { padding: 48px 16px; }
      .section__inner { padding-left: 0; padding-right: 0; }
      .hero { padding: 28px 16px 36px; }

      .section__title { font-size: 28px; margin-bottom: 12px; }
      .section__lead { font-size: 16px; margin-bottom: 32px; }

      .logo__img { height: 52px; }
      .site-header__inner { padding: 10px 16px; gap: 12px; }
      .header-cta { padding: 8px 16px; font-size: 14px; white-space: nowrap; }

      .btn { padding: 14px 22px; font-size: 16px; }
      .hero__subtitle { font-size: 18px; }
      .hero__price strong { font-size: 24px; }
      .hero--premium .hero__actions { flex-direction: column; align-items: stretch; }
      .hero--premium .hero__actions .btn { width: 100%; }

      .footer { padding: 36px 16px 48px; }
      .footer__logo { width: 200px; }

      .benefit:nth-child(n) { transform: none; }
      .benefits__grid::before { display: none; }

      .for-whom__item {
        padding: 88px 20px 20px;
        text-align: center;
      }
      .for-whom__icon-wrap {
        left: 50%; top: 14px;
        transform: translateX(-50%);
      }

      /* Tilda scale — дублируем на мобиле (Tilda иногда перезаписывает после init) */
      .t396,
      .t396__artboard,
      .t396__carrier,
      .t396__filter,
      .tn-elem[data-elem-type="html"] {
        transform: none !important;
        zoom: 1 !important;
        width: 100% !important;
        max-width: 100% !important;
        left: 0 !important;
      }
      .t396__artboard { min-width: 0 !important; }

      /* Блок с «прилипанием» на мобиле ломает скролл */
      .final-cta-scroller { height: auto !important; }
      .final-cta {
        position: relative !important;
        min-height: 620px !important;
        height: auto !important;
        padding: 48px 16px 56px;
      }
      .final-cta__frame {
        opacity: 1 !important;
        transform: none !important;
      }
      .final-cta__bg {
        transform: none !important;
        background-position: center bottom !important;
      }

      .pull-quote {
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 16px;
      }
      .pull-quote__avatar { margin: 0 auto; }

      .enroll-panel { padding: 28px 16px; }
      .tariff-card { padding: 24px 18px; }
    }

    @media (max-width: 480px) {
      .section__title { font-size: 24px; }
      .hero h1 { font-size: 30px; }
      .hero__subtitle { font-size: 17px; }
      .section { padding: 40px 14px; }
      .preview-strip { font-size: 12px; padding: 8px 12px; }
      .acc-head { font-size: 16px; padding: 16px 18px; }
      .tale-card { padding: 18px 16px; }
    }


/* ===== Evergreen school: pillars, catalog, proof ===== */
#chit-main .school-pillars {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  max-width: 960px;
  margin: 36px auto 0;
}
#chit-main .school-pillars__item {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 22px 20px;
  box-shadow: var(--shadow);
}
#chit-main .school-pillars__title {
  margin: 0 0 8px;
  font-size: 18px;
  font-weight: 800;
  color: var(--blue);
}
#chit-main .school-pillars__text {
  margin: 0;
  font-size: 15px;
  line-height: 1.5;
  color: var(--muted);
}
#chit-main .course-catalog {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  margin: 28px 0 12px;
}
#chit-main .course-filters {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 20px 0 4px;
  justify-content: center;
}
#chit-main .course-filter {
  appearance: none;
  border: 1.5px solid var(--border);
  background: var(--white);
  color: var(--text);
  border-radius: 999px;
  padding: 8px 16px;
  font-family: inherit;
  font-size: 14px;
  font-weight: 700;
  cursor: pointer;
  transition: background .15s, border-color .15s, color .15s;
}
#chit-main .course-filter.is-active {
  background: var(--blue);
  border-color: var(--blue);
  color: #fff;
}
#chit-main .course-card {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 0 0 20px;
  overflow: hidden;
  background: var(--white);
  border: 1.5px solid var(--border);
  border-radius: 18px;
  text-decoration: none;
  color: inherit;
  box-shadow: var(--shadow);
  transition: border-color .2s, transform .15s, box-shadow .2s;
}
#chit-main .course-card > :not(.course-card__media) {
  margin-left: 20px;
  margin-right: 20px;
}
#chit-main .course-card > .course-card__badge {
  margin-top: 16px;
}
#chit-main .course-card:hover {
  border-color: #A8C4DD;
  transform: translateY(-2px);
  box-shadow: 0 10px 28px rgba(61, 82, 102, .1);
}
#chit-main .course-card--extra {
  background: linear-gradient(180deg, #FBF8FC 0%, #fff 70%);
}
#chit-main .course-card--early {
  background: linear-gradient(180deg, #F7FBFF 0%, #fff 72%);
}
#chit-main .course-card--upcoming {
  background: linear-gradient(180deg, #FFFCF7 0%, #fff 72%);
}
#chit-main .course-card.is-hidden {
  display: none !important;
}
#chit-main .course-card__media {
  width: 100%;
  aspect-ratio: 1 / 1;
  margin: 0;
  background:
    linear-gradient(145deg, rgba(91, 127, 166, 0.16), rgba(143, 125, 163, 0.18)),
    repeating-linear-gradient(
      -12deg,
      transparent,
      transparent 10px,
      rgba(255, 255, 255, 0.35) 10px,
      rgba(255, 255, 255, 0.35) 12px
    );
  border-bottom: 1px solid var(--border);
}
#chit-main .course-card__badge {
  display: inline-flex;
  align-self: flex-start;
  padding: 4px 10px;
  border-radius: 999px;
  background: var(--blue-pale, #E8F1F8);
  color: var(--blue);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: .02em;
}
#chit-main .course-card--extra .course-card__badge {
  background: var(--accent-pale, #F0EAF5);
  color: var(--accent);
}
#chit-main .course-card--early .course-card__badge {
  background: #E5F3EC;
  color: #3F7A55;
}
#chit-main .course-card--upcoming .course-card__badge {
  background: #F8EFE3;
  color: #9A6B3D;
}
#chit-main .course-card__title {
  font-size: 24px;
  font-weight: 800;
  color: var(--blue);
  margin-top: 4px;
}
#chit-main .course-card__meta,
#chit-main .course-card__status {
  font-size: 14px;
  color: var(--muted);
  font-weight: 600;
}
#chit-main .course-card__status {
  color: #5A8F6D;
}
#chit-main .course-card__status--preorder {
  color: #9A6B3D;
}
#chit-main .course-card__status--soon {
  color: #7A6A8F;
}
#chit-main .course-card__price {
  margin-top: 8px;
  font-size: 18px;
  font-weight: 800;
  color: var(--text);
}
#chit-main .course-card__cta {
  margin-top: auto;
  padding-top: 12px;
  font-size: 14px;
  font-weight: 800;
  color: var(--accent);
}
#chit-main .course-catalog__hub {
  text-align: center;
  margin: 8px 0 28px;
}
#chit-main .course-catalog__hub a {
  color: var(--blue);
  font-weight: 700;
  text-decoration: none;
}
#chit-main .course-catalog__hub a:hover { text-decoration: underline; }

/* Platform tabs */
#chit-main .platform-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  justify-content: center;
  margin: 24px 0 20px;
}
#chit-main .platform-tab {
  appearance: none;
  border: 1.5px solid var(--border);
  background: var(--white);
  color: var(--blue-deep);
  border-radius: 999px;
  padding: 10px 20px;
  font-family: inherit;
  font-size: 15px;
  font-weight: 800;
  cursor: pointer;
  transition: background .15s, border-color .15s, color .15s, box-shadow .15s;
}
#chit-main .platform-tab:hover:not(.is-active) {
  border-color: var(--blue-light);
  background: var(--blue-pale);
  color: var(--blue);
}
#chit-main .platform-tab.is-active {
  background: var(--blue);
  border-color: var(--blue);
  color: #fff;
  box-shadow: 0 6px 18px rgba(91, 127, 166, 0.35);
}
#chit-main .platform-tab.is-active:hover {
  background: var(--blue-deep);
  border-color: var(--blue-deep);
}
#chit-main .platform-panels {
  background: var(--white);
  border: 1.5px solid var(--border);
  border-radius: 24px;
  padding: 28px 24px 32px;
  box-shadow: var(--shadow);
}
#chit-main .platform-panel[hidden] { display: none !important; }
#chit-main .platform-panel__title {
  margin: 0 0 8px;
  text-align: center;
  font-size: 22px;
  font-weight: 800;
  color: var(--blue);
}
#chit-main .platform-panel__lead {
  margin: 0 auto 24px;
  max-width: 640px;
  text-align: center;
  color: var(--muted);
  font-size: 16px;
  line-height: 1.55;
}
#chit-main .proof-product--in-tab {
  margin-top: 8px;
}
#chit-main .proof-stats {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  margin: 28px 0 32px;
}
#chit-main .proof-stats__item {
  text-align: center;
  padding: 20px 12px;
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 16px;
}
#chit-main .proof-stats__num {
  display: block;
  font-size: 34px;
  font-weight: 800;
  color: var(--blue);
  line-height: 1.1;
}
#chit-main .proof-stats__label {
  display: block;
  margin-top: 6px;
  font-size: 14px;
  font-weight: 600;
  color: var(--muted);
}
#chit-main .proof-product {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(0, .8fr);
  gap: 28px;
  align-items: center;
  padding: 28px;
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 20px;
  box-shadow: var(--shadow);
}
#chit-main .proof-product h3 {
  margin: 0 0 10px;
  font-size: 22px;
  color: var(--blue);
}
#chit-main .proof-product p {
  margin: 0 0 18px;
  color: var(--muted);
  line-height: 1.55;
}
#chit-main .proof-product__visual {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  flex-wrap: wrap;
}
#chit-main .proof-product__visual img {
  width: 96px;
  height: 96px;
  object-fit: contain;
}
@media (max-width: 900px) {
  #chit-main .school-pillars { grid-template-columns: 1fr 1fr; }
  #chit-main .course-catalog { grid-template-columns: 1fr; }
  #chit-main .proof-stats { grid-template-columns: 1fr 1fr; }
  #chit-main .proof-product { grid-template-columns: 1fr; }
}
@media (max-width: 560px) {
  #chit-main .school-pillars { grid-template-columns: 1fr; }
}

/* ===== proof reviews + cabinet mock ===== */

#chit-main .proof-reviews {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
  margin: 8px 0 28px;
}
#chit-main .proof-review {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 20px 18px;
}
#chit-main .proof-review__text {
  margin: 0 0 14px;
  font-size: 15px;
  line-height: 1.55;
  color: var(--text);
}
#chit-main .proof-review__author {
  display: block;
  font-style: normal;
  font-size: 13px;
  font-weight: 700;
  color: var(--muted);
}
#chit-main .cabinet-mock {
  margin: 0;
  background: linear-gradient(180deg, #EDF3F9 0%, #F7F5FA 100%);
  border: 1.5px solid var(--border);
  border-radius: 18px;
  overflow: hidden;
  box-shadow: var(--shadow);
}
#chit-main .cabinet-mock__chrome {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 10px 14px;
  background: rgba(255,255,255,.85);
  border-bottom: 1px solid var(--border);
}
#chit-main .cabinet-mock__chrome span {
  width: 8px; height: 8px; border-radius: 50%;
  background: #D4E2EF;
}
#chit-main .cabinet-mock__chrome em {
  margin-left: 8px;
  font-style: normal;
  font-size: 12px;
  font-weight: 800;
  color: var(--blue);
  letter-spacing: .02em;
}
#chit-main .cabinet-mock__body { padding: 18px 16px 16px; }
#chit-main .cabinet-mock__hi strong {
  display: block;
  font-size: 18px;
  color: var(--blue);
}
#chit-main .cabinet-mock__hi span {
  display: block;
  margin-top: 2px;
  font-size: 13px;
  color: var(--muted);
  font-weight: 600;
}
#chit-main .cabinet-mock__stats {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin: 16px 0 12px;
}
#chit-main .cabinet-mock__stats > div {
  background: #fff;
  border-radius: 14px;
  padding: 12px 10px;
  text-align: center;
  border: 1px solid var(--border);
}
#chit-main .cabinet-mock__stats img {
  display: block;
  margin: 0 auto 6px;
  width: 48px;
  height: 48px;
  object-fit: contain;
}
#chit-main .cabinet-mock__stats b {
  display: block;
  font-size: 11px;
  letter-spacing: .04em;
  text-transform: uppercase;
  color: var(--accent);
}
#chit-main .cabinet-mock__stats span {
  display: block;
  margin-top: 2px;
  font-size: 14px;
  font-weight: 800;
  color: var(--blue);
}
#chit-main .cabinet-mock__bar {
  height: 10px;
  border-radius: 999px;
  background: #D8E5F1;
  overflow: hidden;
}
#chit-main .cabinet-mock__bar i {
  display: block;
  height: 100%;
  background: linear-gradient(90deg, #5B7FA6, #8F7DA3);
  border-radius: inherit;
}
#chit-main .cabinet-mock__note {
  margin: 10px 0 0;
  font-size: 12px;
  color: var(--muted);
  font-weight: 600;
}
#chit-main .cabinet-mock figcaption {
  padding: 10px 14px 12px;
  font-size: 12px;
  font-weight: 700;
  color: var(--muted);
  background: rgba(255,255,255,.7);
  border-top: 1px solid var(--border);
}
@media (max-width: 900px) {
  #chit-main .proof-reviews { grid-template-columns: 1fr; }
}

/* Mobile: five levels wrap with horizontal labels */
@media (max-width: 720px) {
  #allrecords #chit-main .gamify-levels,
  #chit-main .gamify-levels {
    display: flex !important;
    flex-wrap: wrap !important;
    justify-content: center !important;
    align-items: flex-start !important;
    gap: 18px 10px !important;
    width: 100% !important;
    max-width: 100% !important;
    padding: 0 12px !important;
    box-sizing: border-box !important;
  }
  #allrecords #chit-main .gamify-levels::before,
  #chit-main .gamify-levels::before,
  #allrecords #chit-main .gamify-levels__arrow,
  #chit-main .gamify-levels__arrow {
    display: none !important;
  }
  #allrecords #chit-main .gamify-levels .gamify-item,
  #chit-main .gamify-levels .gamify-item {
    flex: 0 0 calc(33.333% - 10px) !important;
    min-width: 96px !important;
    max-width: 118px !important;
    width: auto !important;
    gap: 8px !important;
  }
  #allrecords #chit-main .gamify-levels .gamify-item__img,
  #chit-main .gamify-levels .gamify-item__img {
    width: 72px !important;
    height: 72px !important;
    min-width: 72px !important;
    min-height: 72px !important;
    margin: 0 auto !important;
  }
  #allrecords #chit-main .gamify-levels .gamify-item__img img,
  #chit-main .gamify-levels .gamify-item__img img {
    transform: none !important;
  }
  #allrecords #chit-main .gamify-levels .gamify-item__name,
  #chit-main .gamify-levels .gamify-item__name {
    font-size: 12px !important;
    max-width: 100% !important;
    min-height: 0 !important;
    line-height: 1.25 !important;
    white-space: normal !important;
    word-break: normal !important;
    overflow-wrap: normal !important;
    hyphens: none !important;
    text-align: center !important;
    writing-mode: horizontal-tb !important;
  }
}
@media (max-width: 400px) {
  #allrecords #chit-main .gamify-levels .gamify-item,
  #chit-main .gamify-levels .gamify-item {
    flex: 0 0 calc(50% - 8px) !important;
    min-width: 120px !important;
    max-width: 150px !important;
  }
  #allrecords #chit-main .gamify-levels .gamify-item__img,
  #chit-main .gamify-levels .gamify-item__img {
    width: 68px !important;
    height: 68px !important;
    min-width: 68px !important;
    min-height: 68px !important;
  }
}

/* Mobile: split bottom tabs — quiz left, feedback right */
@media (max-width: 720px) {
  #allrecords #chit-main .qz-launcher,
  #chit-main .qz-launcher {
    left: 0 !important;
    right: 50% !important;
    width: auto !important;
    max-width: none !important;
    justify-content: center !important;
    border-radius: 0 !important;
    box-shadow: none !important;
    z-index: 199 !important;
  }
  #allrecords #chit-main .feedback-tab,
  #chit-main .feedback-tab {
    left: 50% !important;
    right: 0 !important;
    width: auto !important;
    max-width: none !important;
    z-index: 200 !important;
  }
  #allrecords #chit-main .feedback-tab__toggle,
  #chit-main .feedback-tab__toggle {
    width: 100% !important;
    justify-content: center !important;
    border-radius: 0 !important;
    box-shadow: none !important;
    border-left: 1px solid rgba(255, 255, 255, 0.28) !important;
    padding: 12px 10px max(14px, env(safe-area-inset-bottom)) !important;
    font-size: 13px !important;
    line-height: 1.2 !important;
    white-space: nowrap !important;
  }
  #allrecords #chit-main .feedback-tab__panel,
  #chit-main .feedback-tab__panel {
    width: min(100vw - 16px, 320px) !important;
    right: 8px !important;
    left: auto !important;
  }
  body.qz-modal-open #chit-main .feedback-tab {
    display: none !important;
  }
}

/* Slovik speech window */
#chit-main .slovik-meet__copy {
  position: relative !important;
  background: #fff !important;
  border: 2px solid #C5D9ED !important;
  border-radius: 20px !important;
  padding: 22px 26px 20px !important;
  box-shadow: 0 10px 28px rgba(91, 127, 166, 0.12) !important;
}
#chit-main .slovik-meet__copy::before {
  content: '' !important;
  position: absolute !important;
  left: -12px !important;
  top: 50% !important;
  width: 18px !important;
  height: 18px !important;
  background: #fff !important;
  border-left: 2px solid #C5D9ED !important;
  border-bottom: 2px solid #C5D9ED !important;
  transform: translateY(-50%) rotate(45deg) !important;
}
#chit-main .slovik-meet__hi strong {
  color: var(--accent) !important;
}
@media (max-width: 720px) {
  #chit-main .slovik-meet__copy::before {
    left: 50% !important;
    top: -10px !important;
    border-bottom: none !important;
    border-top: 2px solid #C5D9ED !important;
    transform: translateX(-50%) rotate(45deg) !important;
  }
}

/* ========== Modern marketplace layer (Skillbox-like, brand colors) ========== */
#chit-main {
  --cream: #F7F8FA;
  --cream-warm: #F0F2F6;
  --border: #E3E8EF;
  --shadow: 0 1px 2px rgba(61, 82, 102, 0.04), 0 8px 24px rgba(61, 82, 102, 0.06);
  --shadow-lg: 0 12px 32px rgba(61, 82, 102, 0.1);
  --radius: 12px;
  background: #fff;
}
#chit-main .paper-bg {
  background-color: #fff !important;
  background-image: none !important;
}
#chit-main .section--cream,
#chit-main .section--catalog {
  background: var(--cream) !important;
}
#chit-main .section__chapter--left,
#chit-main .section__title--left,
#chit-main .section__lead--left {
  text-align: left;
  margin-left: 0;
  margin-right: 0;
}
#chit-main .section__chapter--left {
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-size: 12px;
  font-weight: 800;
  color: var(--accent);
  margin-bottom: 10px;
}
#chit-main .section__title--left {
  font-size: clamp(28px, 4vw, 40px);
  letter-spacing: -0.03em;
  margin-bottom: 10px;
}
#chit-main .section__lead--left {
  max-width: 720px;
  margin: 0 0 28px;
  font-size: 17px;
}

/* Catalog sales layout */
#chit-main .catalog-head {
  display: grid;
  grid-template-columns: minmax(0, 1.35fr) minmax(260px, 0.85fr);
  gap: 18px 32px;
  align-items: center;
  margin-bottom: 18px;
  padding-bottom: 18px;
  border-bottom: 1px solid var(--border);
}
#chit-main .catalog-head .section__lead--left { margin-bottom: 0; }
#chit-main .catalog-help {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 16px 18px;
  border-radius: 12px;
  background: linear-gradient(135deg, #fff 0%, var(--blue-pale) 100%);
  border: 1px solid #C5D9ED;
  text-decoration: none;
  color: inherit;
  transition: border-color .2s, transform .15s, box-shadow .15s;
}
#chit-main .catalog-help:hover {
  border-color: var(--blue-light);
  box-shadow: var(--shadow);
  transform: translateY(-1px);
}
#chit-main .catalog-help__eyebrow {
  font-size: 12px;
  font-weight: 700;
  color: var(--muted);
}
#chit-main .catalog-help__title {
  font-size: 17px;
  font-weight: 800;
  color: var(--blue);
  letter-spacing: -0.02em;
}
#chit-main .catalog-help__hint {
  font-size: 14px;
  font-weight: 700;
  color: var(--accent);
}
#chit-main .course-card__body {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 16px 18px 18px;
  flex: 1;
}
#chit-main .course-card__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  align-items: center;
}
#chit-main .course-card__chip {
  display: inline-flex;
  padding: 4px 10px;
  border-radius: 999px;
  background: #FFF4E8;
  color: #9A6B3B;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.02em;
}
#chit-main .course-card__list {
  margin: 2px 0 4px;
  padding: 0 0 0 18px;
  color: var(--muted);
  font-size: 14px;
  line-height: 1.45;
}
#chit-main .course-card__list li { margin: 0 0 2px; }
#chit-main .course-card__actions {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 8px;
  margin-top: auto;
  padding-top: 8px;
}
#chit-main .course-card__actions--early {
  grid-template-columns: 1fr 1.2fr;
}
#chit-main .course-card__actions--early .course-card__btn--trial {
  grid-column: 1 / -1;
}
#chit-main .course-card__actions--trial .course-card__btn--trial {
  grid-column: 1 / -1;
}
#chit-main .course-card__btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 42px;
  padding: 10px 12px;
  border-radius: 12px;
  background: var(--blue);
  color: #fff !important;
  text-decoration: none;
  font-size: 14px;
  font-weight: 800;
  text-align: center;
  border: none;
  cursor: pointer;
  font-family: inherit;
}
#chit-main .course-card__btn--ghost {
  background: transparent;
  color: var(--blue) !important;
  border: 1.5px solid #C5D9ED;
}
#chit-main .course-card__btn--trial {
  background: #8F7DA3;
  color: #fff !important;
}
#chit-main .course-card__btn:hover { filter: brightness(0.96); }
#chit-main .course-card > .course-card__badge { margin-top: 0; }
#chit-main .course-card > :not(.course-card__media):not(.course-card__body) {
  margin-left: 0;
  margin-right: 0;
}

/* Support pillars */
#chit-main .section--pillars { background: #fff; }
#chit-main .support-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  margin-top: 8px;
}
#chit-main .support-card {
  background: var(--cream);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 22px 18px 20px;
}
#chit-main .support-card__num {
  display: block;
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.08em;
  color: var(--accent);
  margin-bottom: 10px;
}
#chit-main .support-card__title {
  margin: 0 0 8px;
  font-size: 18px;
  font-weight: 800;
  color: var(--blue);
}
#chit-main .support-card__text {
  margin: 0;
  font-size: 15px;
  line-height: 1.5;
  color: var(--muted);
}

/* Reviews as cards */
#chit-main .proof-review--card {
  background: #fff;
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 20px 18px;
  box-shadow: var(--shadow);
}
#chit-main .proof-review__top {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 10px;
}
#chit-main .proof-review__avatar {
  width: 42px;
  height: 42px;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--blue-pale, #E8F1F8);
  color: var(--blue);
  font-weight: 800;
  flex-shrink: 0;
}
#chit-main .proof-review__author {
  display: block;
  font-style: normal;
  font-weight: 800;
  color: var(--text);
  font-size: 15px;
}
#chit-main .proof-review__meta {
  display: block;
  font-size: 13px;
  color: var(--muted);
  font-weight: 600;
}
#chit-main .proof-review__result {
  margin: 0 0 10px;
  font-size: 13px;
  font-weight: 800;
  color: var(--accent);
}

/* Lead form */
#chit-main .section--lead { background: var(--cream); }
#chit-main .lead-panel {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1.1fr);
  gap: 28px;
  align-items: start;
  background: #fff;
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 28px 24px;
  box-shadow: var(--shadow);
}
#chit-main .lead-form__grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  margin-bottom: 14px;
}
#chit-main .lead-form__note {
  margin: 12px 0 0;
  padding: 12px 14px;
  border-radius: 12px;
  background: var(--blue-pale);
  color: var(--blue-deep);
  font-size: 14px;
  font-weight: 700;
  line-height: 1.45;
}
#chit-main .lead-form__note a { color: var(--blue); }
@media (max-width: 900px) {
  #chit-main .catalog-head,
  #chit-main .lead-panel,
  #chit-main .support-grid {
    grid-template-columns: 1fr;
  }
  #chit-main .catalog-head {
    align-items: stretch;
    gap: 14px;
  }
  #chit-main .lead-form__grid { grid-template-columns: 1fr; }
}
@media (max-width: 720px) {
  #chit-main .course-card__actions { grid-template-columns: 1fr; }
}

/* Hero: cleaner, less “shelf” */
#chit-main .hero {
  background: #fff !important;
  padding-top: 48px;
  padding-bottom: 56px;
}
#chit-main .hero.paper-bg {
  background: #fff !important;
  background-image: none !important;
}
#chit-main .hero h1 {
  letter-spacing: -0.04em;
}
#chit-main .hero__showcase {
  border-radius: 16px;
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
  padding: 40px 32px 36px;
}
#chit-main .hero__showcase::after { display: none; }
#chit-main .hero__showcase.is-active {
  border-color: #C5D4E4;
  box-shadow: var(--shadow-lg);
}
#chit-main .book-stack { perspective: none; }
#chit-main .book,
#chit-main .book:nth-child(1),
#chit-main .book:nth-child(2),
#chit-main .book:nth-child(3) {
  transform: none !important;
  border-radius: 8px;
  box-shadow: 0 4px 16px rgba(61, 82, 102, 0.12);
}
#chit-main .book:nth-child(2) {
  transform: translateY(-8px) !important;
}
#chit-main .book.is-active {
  outline: 2px solid var(--blue);
  outline-offset: 3px;
}

/* Benefits: flat row, no stickers tilt */
#chit-main .benefit {
  transform: none !important;
  border-radius: 12px;
  border-top-width: 3px;
  box-shadow: none;
}
#chit-main .benefits {
  background: #fff;
}

/* Filters like marketplace chips */
#chit-main .course-filters {
  justify-content: flex-start;
  gap: 8px;
  margin: 8px 0 20px;
  padding-bottom: 4px;
  overflow-x: auto;
  flex-wrap: nowrap;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}
#chit-main .course-filters::-webkit-scrollbar { display: none; }
#chit-main .course-filter {
  flex: 0 0 auto;
  border-radius: 10px;
  border: 1px solid var(--border);
  background: #fff;
  padding: 10px 16px;
  font-size: 14px;
  font-weight: 700;
  color: var(--text);
}
#chit-main .course-filter.is-active {
  background: var(--blue);
  border-color: var(--blue);
  color: #fff;
}
#chit-main .course-filter:hover:not(.is-active) {
  border-color: var(--blue-light);
  background: var(--blue-pale);
}

/* Course cards: Skillbox-like product tiles */
#chit-main .course-catalog {
  gap: 20px;
  margin: 0 0 20px;
}
#chit-main .course-card,
#chit-main .course-card--early,
#chit-main .course-card--extra,
#chit-main .course-card--upcoming {
  background: #fff !important;
  border: 1px solid var(--border);
  border-radius: 12px;
  box-shadow: none;
  padding: 0 0 18px;
  gap: 4px;
  transition: border-color .15s, box-shadow .15s, transform .15s;
}
#chit-main .course-card:hover {
  border-color: #B8C9DB;
  box-shadow: var(--shadow);
  transform: translateY(-3px);
}
#chit-main .course-card__media {
  aspect-ratio: 16 / 10;
  border-bottom: none;
  background:
    linear-gradient(135deg, #E8F1F8 0%, #EBE6F3 100%);
  position: relative;
  overflow: hidden;
  border-radius: 12px 12px 0 0;
}
#chit-main .course-card__media::after {
  content: '';
  position: absolute;
  inset: 18% 22%;
  border-radius: 8px;
  border: 1.5px dashed rgba(91, 127, 166, 0.35);
  background: rgba(255, 255, 255, 0.35);
}
#chit-main .course-card--early .course-card__media {
  background: linear-gradient(135deg, #E5F3EC 0%, #E8F1F8 100%);
}
#chit-main .course-card--extra .course-card__media {
  background: linear-gradient(135deg, #EBE6F3 0%, #F0F2F6 100%);
}
#chit-main .course-card--upcoming .course-card__media {
  background: linear-gradient(135deg, #F8EFE3 0%, #E8F1F8 100%);
}
#chit-main .course-card[data-group="early-letters"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-letters.jpg?v=20260822g") center center / cover no-repeat !important;
}
#chit-main .course-card[data-group="early-stories"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-stories.jpg?v=20260822g") center center / cover no-repeat !important;
}
#chit-main .course-card[data-group="early-letters"] .course-card__media img,
#chit-main .course-card[data-group="early-stories"] .course-card__media img {
  object-position: center center !important;
}
#chit-main .course-card[data-group="early-letters"] .course-card__media::after,
#chit-main .course-card[data-group="early-stories"] .course-card__media::after,
#chit-main .course-card[data-group="grade-1"] .course-card__media::after,
#chit-main .course-card[data-group="grade-2"] .course-card__media::after,
#chit-main .course-card[data-group="grade-3"] .course-card__media::after,
#chit-main .course-card[data-group="grade-4"] .course-card__media::after,
#chit-main .course-card[data-group="extra-6-8"] .course-card__media::after,
#chit-main .course-card[data-group="extra-9-11"] .course-card__media::after,
#chit-main .course-card[data-cover] .course-card__media::after,
#chit-main .course-card[data-course-title="Ветер в ивах"] .course-card__media::after,
#chit-main .course-card[data-course-title="Таинственный сад"] .course-card__media::after,
#chit-main .course-card[data-course-title="Русские сказки"] .course-card__media::after {
  display: none !important;
}
#chit-main .course-card[data-group="grade-1"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-grade-1.jpg") center 40% / cover no-repeat !important;
}
#chit-main .course-card[data-group="grade-2"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-grade-2.jpg") center 42% / cover no-repeat !important;
}
#chit-main .course-card[data-group="grade-3"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-grade-3.jpg") center 42% / cover no-repeat !important;
}
#chit-main .course-card[data-group="grade-4"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-grade-4.jpg") center 42% / cover no-repeat !important;
}
#chit-main .course-card[data-group="extra-6-8"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-extra-6-8.jpg") center 45% / cover no-repeat !important;
}
#chit-main .course-card[data-group="extra-9-11"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-extra-9-11.jpg") center 45% / cover no-repeat !important;
}
#chit-main .course-card[data-cover="wind"] .course-card__media,
#chit-main .course-card[data-course-title="Ветер в ивах"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-wind.jpg") center 48% / cover no-repeat !important;
}
#chit-main .course-card[data-cover="garden"] .course-card__media,
#chit-main .course-card[data-course-title="Таинственный сад"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-garden.jpg") center 45% / cover no-repeat !important;
}
#chit-main .course-card[data-cover="rus-6-9"] .course-card__media,
#chit-main .course-card[data-course-title="Русские сказки"][data-course-meta="6–9 лет · старт в октябре"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-rus-6-9.jpg") center 48% / cover no-repeat !important;
}
#chit-main .course-card[data-cover="rus-10-12"] .course-card__media,
#chit-main .course-card[data-course-title="Русские сказки"][data-course-meta="10–12 лет · старт в октябре"] .course-card__media {
  background: url("https://api.chitatelstvo.ru/assets/course-cover-rus-10-12.jpg") center 42% / cover no-repeat !important;
}
#chit-main .course-card__media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 45%;
  display: block;
  position: relative;
  z-index: 1;
  border-radius: 12px 12px 0 0;
}
#chit-main .course-card__media:has(img)::after { display: none; }
#chit-main .course-card > :not(.course-card__media):not(.course-card__body) {
  margin-left: 16px;
  margin-right: 16px;
}
#chit-main .course-card > .course-card__badge {
  margin-top: 14px;
}
#chit-main .course-card__body {
  margin-left: 0 !important;
  margin-right: 0 !important;
}
#chit-main .course-card__badge {
  border-radius: 6px;
  padding: 3px 8px;
  font-size: 11px;
  letter-spacing: 0.02em;
  text-transform: uppercase;
}
#chit-main .course-card__title {
  font-size: 20px;
  letter-spacing: -0.02em;
  margin-top: 2px;
  color: var(--text);
}
#chit-main .course-card__meta {
  font-size: 13px;
  font-weight: 600;
}
#chit-main .course-card__status {
  font-size: 13px;
}
#chit-main .course-card__price {
  margin-top: 10px;
  font-size: 18px;
  color: var(--text);
}
#chit-main .course-card__cta {
  color: var(--blue);
  font-size: 14px;
  padding-top: 8px;
}
#chit-main .course-catalog__hub {
  text-align: left;
  margin: 4px 0 36px;
}

/* Tariffs / platform: flatter */
#chit-main .tariff-card,
#chit-main .platform-panels,
#chit-main .proof-stats__item,
#chit-main .proof-product {
  border-radius: 12px;
  box-shadow: none;
  border-color: var(--border);
}
#chit-main .platform-tab,
#chit-main .btn,
#chit-main .header-cta {
  border-radius: 10px;
}
#chit-main .platform-tabs {
  justify-content: flex-start;
}

@media (max-width: 900px) {
  #chit-main .course-catalog { grid-template-columns: 1fr; }
  #chit-main .section__chapter--left,
  #chit-main .section__title--left,
  #chit-main .section__lead--left,
  #chit-main .course-catalog__hub {
    text-align: left;
  }
}

/* Fare / tariff picker (airline-style cards) */
#chit-main .fare-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 8px;
}
#chit-main .fare-head .section__title--left { margin-bottom: 0; }
#chit-main .fare-head__link {
  font-size: 14px;
  font-weight: 700;
  color: var(--accent);
  text-decoration: none;
  white-space: nowrap;
}
#chit-main .fare-head__link:hover { text-decoration: underline; }
#chit-main .fare-track {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
  margin-top: 20px;
  align-items: stretch;
}
#chit-main #tariffs .fare-track {
  padding-top: 10px;
}
#chit-main #tariffs .fare-card__radio {
  display: none;
}
#chit-main #tariffs .fare-card--rec {
  padding-top: 36px;
  border-color: #B8D4BE;
}
#chit-main #tariffs .fare-card__badge {
  top: 10px;
  left: 50%;
  transform: translateX(-50%);
  padding: 5px 12px;
  font-size: 11px;
  letter-spacing: 0.02em;
  text-transform: uppercase;
  box-shadow: 0 1px 3px rgba(47, 107, 58, 0.12);
}
#chit-main .fare-card,
.fare-modal .fare-card {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  text-align: left;
  gap: 4px;
  padding: 22px 18px 18px;
  border-radius: 16px;
  border: 1.5px solid var(--border);
  background: #fff;
  box-shadow: none;
  cursor: pointer;
  font: inherit;
  color: inherit;
  transition: border-color .15s, box-shadow .15s, transform .15s;
}
#chit-main .fare-card:hover,
.fare-modal .fare-card:hover {
  border-color: #B8C9DB;
  box-shadow: var(--shadow);
  transform: translateY(-2px);
}
#chit-main .fare-card.is-selected,
.fare-modal .fare-card.is-selected {
  border-color: var(--accent);
  box-shadow: 0 0 0 1px var(--accent);
}
#chit-main .fare-card--rec,
.fare-modal .fare-card--rec { padding-top: 36px; }
#chit-main .fare-card__badge,
.fare-modal .fare-card__badge {
  position: absolute;
  top: 10px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 2;
  padding: 5px 12px;
  border-radius: 999px;
  background: #E6F4EA;
  color: #2F6B3A;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.02em;
  text-transform: uppercase;
  white-space: nowrap;
  line-height: 1.2;
  box-shadow: 0 1px 3px rgba(47, 107, 58, 0.12);
  pointer-events: none;
}
#chit-main .fare-card__radio,
.fare-modal .fare-card__radio {
  width: 22px;
  height: 22px;
  border-radius: 50%;
  border: 2px solid #C5D0DC;
  margin-bottom: 10px;
  flex-shrink: 0;
  position: relative;
  background: #fff;
}
#chit-main .fare-card.is-selected .fare-card__radio,
.fare-modal .fare-card.is-selected .fare-card__radio {
  border-color: var(--accent);
}
#chit-main .fare-card.is-selected .fare-card__radio::after,
.fare-modal .fare-card.is-selected .fare-card__radio::after {
  content: '';
  position: absolute;
  inset: 4px;
  border-radius: 50%;
  background: var(--accent);
}
#chit-main .fare-card__name,
.fare-modal .fare-card__name {
  display: block;
  font-size: 22px;
  font-weight: 800;
  color: var(--text);
  letter-spacing: -0.02em;
  line-height: 1.2;
}
#chit-main .fare-card__price,
.fare-modal .fare-card__price {
  display: block;
  margin-top: 6px;
  font-size: 20px;
  font-weight: 800;
  color: var(--text);
}
#chit-main .fare-card__delta,
.fare-modal .fare-card__delta {
  display: block;
  font-size: 13px;
  font-weight: 700;
  color: var(--muted);
}
#chit-main .fare-card__sub,
.fare-modal .fare-card__sub {
  display: block;
  margin: 2px 0 10px;
  font-size: 13px;
  font-weight: 600;
  color: var(--muted);
}
#chit-main .fare-card__feats,
.fare-modal .fare-card__feats {
  list-style: none;
  margin: 0 0 14px;
  padding: 0;
  width: 100%;
  flex: 1;
}
#chit-main .fare-card__feats li,
.fare-modal .fare-card__feats li {
  position: relative;
  padding: 5px 0 5px 28px;
  font-size: 14px;
  line-height: 1.35;
  color: var(--text);
  font-weight: 600;
}
#chit-main .fare-card__feats li::before,
.fare-modal .fare-card__feats li::before {
  content: '';
  position: absolute;
  left: 0;
  top: 7px;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: #E6F4EA;
}
#chit-main .fare-card__feats li.is-yes::after,
.fare-modal .fare-card__feats li.is-yes::after {
  content: '';
  position: absolute;
  left: 5px;
  top: 11px;
  width: 8px;
  height: 5px;
  border-left: 2px solid #2F6B3A;
  border-bottom: 2px solid #2F6B3A;
  transform: rotate(-45deg);
}
#chit-main .fare-card__feats li.is-no::before,
.fare-modal .fare-card__feats li.is-no::before {
  background: #F0F2F5;
}
#chit-main .fare-card__feats li.is-no::after,
.fare-modal .fare-card__feats li.is-no::after {
  content: '×';
  position: absolute;
  left: 0;
  top: 5px;
  width: 18px;
  text-align: center;
  font-size: 14px;
  font-weight: 800;
  color: #8A96A3;
  line-height: 18px;
}
#chit-main .fare-card__feats li.is-no {
  color: var(--muted);
  font-weight: 600;
}
#chit-main .fare-card__cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  min-height: 42px;
  margin-top: auto;
  padding: 10px 14px;
  border-radius: 12px;
  background: var(--blue);
  color: #fff !important;
  text-decoration: none;
  font-size: 14px;
  font-weight: 800;
}
#chit-main .fare-card__cta:hover { filter: brightness(0.96); }
#chit-main .course-card { cursor: pointer; }
#chit-main .course-card a[data-course-detail] { cursor: pointer; }

.fare-modal .fare-card.is-disabled {
  opacity: 0.42;
  pointer-events: none;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}
.fare-modal .fare-card.is-disabled:hover {
  border-color: var(--border);
  transform: none;
  box-shadow: none;
}
.fare-modal {
  position: fixed;
  inset: 0;
  z-index: 10050;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px 14px;
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
  transition: opacity .2s, visibility .2s;
}
.fare-modal.is-open {
  opacity: 1;
  visibility: visible;
  pointer-events: auto;
}
.fare-modal__backdrop {
  position: absolute;
  inset: 0;
  background: rgba(45, 58, 74, 0.48);
}
.fare-modal__dialog {
  position: relative;
  z-index: 1;
  width: min(960px, 100%);
  max-height: min(92vh, 860px);
  overflow: auto;
  background: #F7F8FA;
  border-radius: 20px;
  padding: 28px 24px 22px;
  box-shadow: 0 24px 64px rgba(45, 58, 74, 0.28);
}
.fare-modal__close {
  position: absolute;
  top: 14px;
  right: 14px;
  width: 36px;
  height: 36px;
  border: none;
  border-radius: 50%;
  background: #fff;
  color: var(--blue-deep, #4A6D94);
  box-shadow: 0 2px 10px rgba(61,82,102,.12);
  cursor: pointer;
}
.fare-modal__close-icon {
  display: block;
  width: 14px;
  height: 14px;
  margin: 0 auto;
  position: relative;
}
.fare-modal__close-icon::before,
.fare-modal__close-icon::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 0;
  width: 14px;
  height: 2px;
  background: currentColor;
  border-radius: 1px;
}
.fare-modal__close-icon::before { transform: translateY(-50%) rotate(45deg); }
.fare-modal__close-icon::after { transform: translateY(-50%) rotate(-45deg); }
.fare-modal__head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 18px;
  padding-right: 40px;
}
.fare-modal__eyebrow {
  margin: 0 0 4px;
  font-size: 13px;
  font-weight: 800;
  color: var(--muted, #6B8499);
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.fare-modal__title {
  margin: 0;
  font-size: clamp(24px, 3vw, 32px);
  font-weight: 800;
  color: var(--text, #3D5266);
  letter-spacing: -0.03em;
}
.fare-modal__course {
  margin: 6px 0 0;
  font-size: 15px;
  font-weight: 700;
  color: var(--blue, #5B7FA6);
}
.fare-modal__rules {
  font-size: 14px;
  font-weight: 700;
  color: var(--accent, #8F7DA3);
  text-decoration: none;
  white-space: nowrap;
  margin-top: 4px;
}
.fare-modal .fare-track--modal {
  display: grid;
  grid-template-columns: repeat(3, minmax(220px, 1fr));
  gap: 12px;
  overflow-x: auto;
  padding: 4px 2px 4px;
  -webkit-overflow-scrolling: touch;
}
.fare-modal .fare-card--rec { padding-top: 36px; }
.fare-modal .fare-card__badge {
  top: 10px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 2;
}
.fare-modal__foot {
  margin-top: 18px;
  display: grid;
  gap: 10px;
}
.fare-modal__foot .btn {
  max-width: 420px;
  margin: 0 auto;
  border-radius: 12px;
}
.fare-modal__note {
  margin: 0;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  color: var(--muted, #6B8499);
  line-height: 1.4;
}
body.fare-modal-open { overflow: hidden; }
@media (max-width: 900px) {
  #chit-main .fare-track {
    grid-template-columns: 1fr;
    max-width: 420px;
  }
  .fare-modal .fare-track--modal {
    grid-template-columns: repeat(3, minmax(240px, 78vw));
  }
  .fare-modal__head { flex-direction: column; gap: 8px; }
  .fare-modal__dialog { padding: 22px 14px 16px; }
}


